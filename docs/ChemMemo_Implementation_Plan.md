---
type: plan
project: ChemMemo
title: ChemMemo — Implementation Plan (Phases 0–10 + Progress Log)
status: shipped — live in production
aliases:
  - ChemMemo Implementation Plan
tags:
  - chememo
  - chememo/plan
  - chememo/log
created: 2026-07-01
updated: 2026-07-21
---

# ChemMemo — Implementation Plan

Hub: [[ChemMemo]] · Vision: [[ChemMemo_Characterization]]

> **How to use this note across sessions:**
> 1. Read the **Progress log** first — it says exactly where we left off.
> 2. Work the current phase's checklist; tick boxes as you finish.
> 3. Before ending a session, update **Progress log** (status table + a dated line) and `next_action` in [[ChemMemo]].
> Each step has a **verify** — don't tick the box until the verify passes.

---

## 🔑 API-key policy (IMPORTANT — decided 2026-07-01)
**No AI API keys are added until the very end.** We build a fully working, keyless lab notebook first (auth, database, structured entry, file linking, exact/filter search, deploy). All AI-feature *code* (embeddings, RAG, summaries, LLM-assisted entry) is written **inert** — it does not run until keys exist. Adding the Anthropic + OpenAI keys and switching the AI on is the **last phase (Phase 10)**. This lets everything get built now without buying a key yet.

## Stack at a glance
- **Frontend:** Next.js (App Router) + React + TypeScript, Tailwind CSS. Port the `chemmemo-design/` mockup.
- **Backend:** Supabase — Postgres + Auth + Storage + `pgvector`.
- **AI (activated Phase 10):** **switchable provider** via `AI_PROVIDER` env (gemini | openai | anthropic — all three code paths kept). **Currently active: Gemini** (`gemini-flash-latest` chat + `gemini-embedding-001` @ 1536 dims), one free key. Switch fast by setting `AI_PROVIDER` + the matching key (`claude-sonnet-5` / `text-embedding-3-small` remain wired). Embeddings always 1536 dims so the `vector(1536)` schema never changes.
- **Retrieval:** hybrid — deterministic SQL filters (keyless) + pgvector semantic search (needs key). The keyless filter path works with no AI at all.
- **Deploy:** Railway (frontend) + Supabase (managed backend). Secrets in Railway env + `.env.local` (never committed).
- **Tooling confirmed present (2026-07-01):** node 24, npm 11, git 2.52, gh 2.95, supabase 2.108. Railway CLI 5.23.3 installed + authenticated (2026-07-02), `use-railway` skill installed. Supabase MCP + Context7 + chrome-devtools + github MCP connected.

---

## Progress log

**Status table** (update each session):

| Phase | Title | Status |
|---|---|---|
| 0 | Foundations & accounts (no AI keys) | ✅ done (2026-07-02) |
| 1 | Auth + app shell | ✅ done (2026-07-02) |
| 2 | Data model & DB (schema + RLS) | ✅ done on dev (2026-07-02) · prod schema deferred |
| 3 | Experiment CRUD UI | ✅ done on dev (2026-07-05) |
| 4 | File handling | ✅ done on dev (2026-07-05) |
| 5 | Keyless search + deploy the MVP | ✅ done on dev (2026-07-05) — keyless MVP shipped |
| 6 | Embeddings pipeline 🔑 *(code only; runs in P10)* | ✅ code done on dev (2026-07-13) |
| 7 | Ask AI — hybrid RAG 🔑 *(code only; runs in P10)* | ✅ code done on dev (2026-07-14) |
| 8 | AI summaries 🔑 *(code only; runs in P10)* | ✅ code done on dev (2026-07-14) |
| 9 | LLM-assisted entry 🔑 *(code only; runs in P10)* | ✅ code done on dev (2026-07-14) |
| 10 | **Add AI API keys → activate & verify AI (LAST)** | ✅ done — activated via **Gemini** (2026-07-15); eval set done (S5); **promoted to production 2026-07-20** |

Legend: ⬜ not started · 🟡 in progress · ✅ done · 🔑 key-gated (build now, activate in Phase 10)

**Session history** (newest first — append a line each session):
- 2026-07-28 — **T0.11 shipped on dev (protect seed/reference project deletion)** — eleventh item of [[ChemMemo_Product_Evolution_Plan]] Tier 0, small (`P2`), no spec needed (model: Sonnet 5). Read T0.11's acceptance criteria and presented a short plan before coding, per this project's standing pattern. **Found a real precondition gap while checking it:** the plan assumed seed cleanup was done (true on prod — 0 ownerless rows), but **dev still had all 4 seed projects** (`wet-dry`/`depsi`/`lcms`/`micro`) ownerless and actively referenced by 13 seed experiments (EXP-001..013) — tightening the RLS policy without addressing this would have permanently stranded them (no one could ever delete or reassign them again). Asked the user how to handle it; they chose to backfill ownership rather than leave them ownerless or pause the item. A second real ambiguity — two candidate accounts looked like the user's own (`yishieze@gmail.com`, last sign-in 2026-07-27, vs `yishi.ezerzer@mail.huji.ac.il`, last sign-in 2026-07-02) — was also asked rather than guessed; user picked `yishieze@gmail.com`. New migration `20260728120000_tighten_project_delete.sql`: backfills `owner_id` on any null-owner row to that account (no-op on prod, which already has none), then drops `projects_delete_own` and recreates it with `owner_id = auth.uid()` only (no more `or owner_id is null`). `sidebar-nav.tsx:131`'s `canDelete` updated to match. Applied via `supabase db query --linked --file <migration>` rather than `db push`/`migration repair`, deliberately avoiding touching this project's known pre-existing migration-history-vs-local-filename drift (documented in the 2026-07-21 entry below) — that drift is out of this item's scope and repairing it would rewrite remote migration-history bookkeeping, a real decision left alone rather than bundled in. **Verified via direct RLS test, not just UI** (no browser login credentials for the real account on hand): signed in as the existing E2E test user via the auth token endpoint, confirmed that account could **not** delete `wet-dry` (owned by the other user) — 200 response, 0 rows affected, row unchanged — then created a disposable throwaway project owned by the E2E user and confirmed that account **could** delete its own project (200, row gone), cleaning up the temp token file afterward. `npm run build` green. **Not yet promoted to master/prod** — dev-only per the plan's tier-by-tier gate, matching T0.1–T0.9.
- 2026-07-27 — **T0.10 code shipped on dev, live verification paused (production health & index-health screen)** — tenth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5). Flagged two real decisions before writing code, since this item is unlike T0.1–T0.9 (not a pure dev-environment change): (1) "admin-only screen" has no admin/role concept to hook into (schema/types grepped, confirmed none exists — that's T2.1's future job) — user chose **any authenticated user**, matching the app's existing lab-shared trust model; (2) the spec's backup-restoration test needs a real backup to restore from — checked prod's actual capability via `supabase backups list --project-ref iazuubcyxneavrahjgww` (read-only, no changes) and found **zero backups, PITR off** — a real restore test needs a paid Supabase plan upgrade, a cost/billing decision left to the user, who chose to **record it as tracked residual debt** rather than force that decision now. Built `lib/health/service.ts` (shared by the existing T0.9 `/api/health` endpoint, refactored to remove duplication, and a new `/health` screen): index-job queue depth/failures + a list of failed jobs with their last error, embedding index model/dims/coverage, recent AI errors, and a manually-tracked `BACKUP_TEST_STATUS` constant documenting the real gap honestly instead of faking a test that hasn't happened. Added a sidebar nav link. Verified: typecheck/lint/unit-tests/build all green; pushed to `dev`; confirmed both `ci` and `rls` GitHub Actions jobs green at commit `fdc89a7` (including a full Playwright E2E run against a fresh build); confirmed via `railway status`/`railway logs` that the correct commit deployed with a clean server boot. **Final live browser click-through of the new screen is blocked**: the university network is currently blocking the `*.up.railway.app` domain entirely (confirmed by Yishi after investigating what looked like a TLS/certificate error from both `curl` and the browser tool, on a domain that had worked all session — also briefly saw a transient "Something went wrong" Server Components error on **prod**, unrelated to this dev-only work, which had already self-resolved on retry before the network block was identified as the real cause). Paused here per Yishi's direction to wait for network access to return before any further live verification or next Tier 0 item.
- 2026-07-26 — **T0.9 shipped on dev (observability: structured logs + health endpoint)** — ninth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5). Asked the user Sentry-vs-equivalent before writing code (a genuine external-service decision — new third-party account + dependency vs. none); user chose **structured logs only, no Sentry**. Added `lib/logger.ts` (JSON lines to stdout — `level`/`context`/`message`/`timestamp`/optional `traceId`/`meta`; Railway already captures and parses stdout into searchable fields, confirmed live via `railway logs`) and replaced all 8 app-code `console.error`/`console.log` call sites with it (`lib/errors.ts`, `app/api/ask/route.ts`, `lib/index-jobs.ts` ×3, `lib/ai/service.ts`, `lib/experiments/service.ts` ×4); left the 10 in one-off `scripts/*.ts` CLI tools alone (not part of the running server). Wired `instrumentation.ts`'s `onRequestError` — Next.js's official catch-all for otherwise-unhandled server errors (route handlers/Server Components/Server Actions), the same integration point a Sentry SDK would use — so even an error that never passed through T0.8's `AppError`/`toActionResult` path still gets a trace ID and a structured log line. Added unauthenticated `GET /api/health` (DB reachability via a cheap query, `index_jobs` pending/failed counts from T0.5, a recent `ai_requests` error-rate sample from T0.3 — aggregate counts only, no PII/secrets/prompt text; a visual dashboard over this data is explicitly T0.10, not this item). **Caught a real bug during live verification**: the global auth middleware (`lib/supabase/middleware.ts`'s `PUBLIC_PATHS`) redirected `/api/health` to `/login` for unauthenticated callers — exactly the audience a health endpoint exists for — fixed by adding `/api/health` to `PUBLIC_PATHS`, verified the fix live (200 with real JSON body: `status:"ok"`, `db.ok:true`, `indexJobs:{pending:0,failed:0}`, `ai.recentSampleSize:7`). Verified: typecheck/lint/unit tests/build all green after each of the three pushes; CI's `ci`+`rls` GitHub Actions jobs green on all three commits (the middleware-fix push too); confirmed the deployed commit hash matched Railway each time; confirmed a real structured log line (`poller started`) visible and field-parsed in `railway logs`. **Not yet promoted to master/prod** — dev-only per the plan's tier-by-tier gate, matching T0.1–T0.8.
- 2026-07-26 — **T0.8 shipped on dev (domain service layer + typed error model)** — eighth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0, not spec-first but the largest Tier 0 refactor so far (model: Sonnet 5). Extracted business logic out of every server action into new service modules: `lib/experiments/service.ts` (merged the old `lib/experiments.ts` reads with create/update/soft-delete moved from `new/actions.ts`), `lib/files/service.ts` (upload/link/remove + validators from `file-actions.ts`), `lib/projects/service.ts` (create/delete from `projects-actions.ts`, plus `listProjects` relocated here from `lib/experiments.ts` since it's project data), `lib/ai/service.ts` (the rate-limit → generate → log `ai_requests` sequence that was duplicated three separate times across `ask/actions.ts`, `summary-actions.ts`, and `api/ask/route.ts`, each with its own slightly-different local `logAiRequest`). Added `lib/authorization/policies.ts`'s `requireUser()` — deliberately just the repeated auth boilerplate, no new permission logic beyond what RLS already enforces (T2.1's job, not this item's). Added `lib/errors.ts`'s typed `AppError` (validation/permission-denied/not-found/conflict/rate-limited/provider-unavailable/index-pending, each with a `crypto.randomUUID()` trace ID) + `toActionResult()`, replacing ad hoc `{ok:false,error}` string literals with no taxonomy; the route handler reuses the same `AppError`/`HTTP_STATUS_FOR_CODE` map for its non-`ActionResult` HTTP responses. **One deliberate scope deviation from the plan's literal file list**, flagged to the user before starting and approved: left `lib/search.ts`/`lib/rag.ts` at their existing paths rather than renaming into `lib/search/service.ts` — they're already action-free, well-factored modules, so a path rename would only churn ~5 importers for no behavioral benefit. **One deliberate behavior preservation caught during the refactor**: `ask/actions.ts`'s `generateGroupSummary` returns `null` on a missing session instead of redirecting (unlike every other action) — a pre-existing, intentional soft-failure for a background call from an in-progress Ask conversation; kept it that way rather than switching to `requireUser()`'s redirect, which would have been a real behavior change disguised as a refactor. Verified thoroughly given the scope: `npm run typecheck`/`lint`/`test`/`build` all green; a full local Playwright E2E run (7/7 green, including the create+delete-experiment and search flows that exercise the refactored paths); pushed to `dev` and confirmed **both `ci` and `rls` GitHub Actions jobs fully green** (the RLS suite needs Docker, unavailable on this dev machine, so this was the only way to prove authorization behavior held); then browser-verified live on the dev URL every flow not covered by the automated E2E suite — grounded Ask + group summary, single-experiment summary generation, project create/delete, experiment create, file-link add/remove — all worked correctly, with test artifacts (throwaway project + experiment) cleaned up afterward. **Not yet promoted to master/prod** — dev-only per the plan's tier-by-tier gate, matching T0.1–T0.7.
- 2026-07-26 — **T0.7 shipped on dev (generated DB types)** — seventh item of [[ChemMemo_Product_Evolution_Plan]] Tier 0, a small mechanical item, no spec needed (model: Sonnet 5). Added `npm run types:gen` (`supabase gen types typescript --linked`, dev project already linked) and committed the output as `lib/database.types.ts` (8 tables + the `match_experiments` RPC). Parametrized all three Supabase client factories (`lib/supabase/{server,client,admin}.ts`) with the generated `Database` type. Rewrote `lib/types.ts` so `Project`/`Experiment`/`ExperimentFile`/`ExperimentRevision` are derived from the generated Row types instead of hand-copied — narrowing a handful of columns (`compounds`/`metals`/`methods`/`mz`, `created_at`/`updated_at`, `experiment_files.kind`, `experiment_revisions.snapshot`) that are DB-nullable/generic-`Json` but always set by every write path, with a comment explaining why each narrowing is safe. Fixed the resulting typecheck fallout at data-access query boundaries (`lib/experiments.ts`, `lib/search.ts`, `lib/rag.ts`, `lib/sync-embedding.ts`, `ask/actions.ts`, `summary-actions.ts`) with documented casts at each Supabase-query return site — left UI components untouched since they consume the same exported type names unchanged. **Found and fixed a genuine latent bug while fixing fallout:** `lib/sync-embedding.ts` was sending a raw `number[]` for the `experiment_embeddings.embedding` pgvector column instead of the JSON-stringified vector every other call site in the codebase already used (e.g. `lib/rag.ts`'s `query_embedding`) — worked before only because the client was untyped. **Proved the acceptance criterion live**: temporarily renamed a column in the generated types file, confirmed `tsc` surfaced ~17 real errors across the app, then regenerated from the real schema and confirmed clean again. Verified: `npm run typecheck`/`lint`/`test`/`build` all green, pushed to `dev`, confirmed Railway deployed the exact commit, and smoke-tested the live dev URL (unauthenticated routes correctly redirect to `/login`, login page 200s). **Not yet promoted to master/prod** — dev-only per the plan's tier-by-tier gate, matching T0.1–T0.6.
- 2026-07-26 — **T0.6 completed and fully shipped (CI + branch protection live)** — closing out T0.6 the same session it was extended (model: Sonnet 5). Added the last pieces: Testing Library + jsdom component tests (`ToastProvider`/`useToast`, with a `tests/setup.ts` wiring Testing Library's `cleanup()` between tests — Vitest needs this explicitly, unlike Jest's default), a new `tests/rls/experiments.rls.test.ts` proving the current lab-shared model's real isolation boundary (anonymous reads blocked entirely, any authenticated user can read, only the owner can write/delete/insert-as-self) using real `@supabase/supabase-js` calls against a local Supabase instance rather than pgTAP (chose the lower-risk option given zero ability to dry-run pgTAP syntax without Docker locally — reused the exact admin-API test-user pattern already proven working this session), a new CI `rls` job (Docker + Supabase CLI, available on GitHub Actions runners even though not on this dev machine; `supabase start` applying every migration cleanly doubles as migration validation), a non-blocking `npm audit` step (blocking would immediately re-break CI on the same pre-existing `next`/`eslint-config-next` findings noted all session — out of scope to fix here), and `gitleaks` secret scanning. **First `rls` job run failed** with "permission denied for table experiments" — a genuine, non-obvious finding: hosted Supabase projects get default table GRANTs to `anon`/`authenticated` from the platform's own project-creation bootstrap, which isn't captured in any migration file, so a bare `supabase start` (CLI-tracked migrations only) doesn't replicate it. Fixed with an explicit `GRANT` step scoped to the CI job's ephemeral local instance only — deliberately did not touch any real migration, since dev/prod already work fine (they got the grant from initial project creation, not from a migration). Re-pushed, confirmed both `ci` and `rls` jobs fully green. Finally applied branch protection on `master` (user confirmed: require `ci`+`rls` status checks, no PR review required, force-push/delete disabled) via `gh api` — flagged to the user that this likely means future `dev`→`master` promotions need to go through a GitHub PR (even without a review) rather than a raw direct push, since required-status-checks generally can't gate a push that's never had CI run on its exact commit. **All of T0.6's stated acceptance criteria are now met and CI-verified**, closing out the largest, most infrastructure-heavy item in Tier 0. E2E coverage remains narrower than the "Do" line's full flow wishlist (edit/clone/complete, file upload, Ask AI, project permissions, revision display, mobile nav) — treated as normal incremental follow-on work, not a blocker, since the testing/CI *infrastructure* itself is what was foundational and is now complete. **Not yet promoted to master/prod as app code** (T0.1–T0.6 all still dev-only).
- 2026-07-26 — **T0.6 extended on dev (Playwright E2E + axe, CI now fully green)** — continuing the prior session's T0.6 phase 1+2 (model: Sonnet 5). Asked the user how to source E2E login credentials rather than guessing (creating persistent auth state touches the shared dev DB); they chose "create via Supabase admin API" — new `scripts/create-e2e-user.ts` provisions a dedicated `e2e-test@chememo.local` account, its credentials + the app's Supabase URL/anon/service-role keys stored as GitHub Actions secrets (mirroring what's already in Railway's dev env — same trust boundary). Installed Playwright + Chromium locally (nothing was cached), wrote 7 real E2E tests: unauthenticated redirect, login form render, sign in→dashboard→sign out, create+delete an experiment, global search, and axe accessibility scans (login + experiments list). Found and fixed two real bugs while writing these: (1) `app/(app)/loading.tsx` had `aria-label` on a role-less `<div>` — a genuine WCAG 4.1.2 violation axe caught immediately, fixed with `role="status"`; (2) all specs share one test account, and running them across Playwright's default parallel workers raced concurrent logins into corrupting each other's sessions — pinned `workers: 1`. Also discovered (not a bug, but worth noting) that the login/experiment-form `<label>` elements aren't programmatically associated with their inputs (no `htmlFor`/`id`), so `getByLabel` silently fails to find them — tests use placeholder/role locators instead; flagged as real debt for the future T1.10 accessibility pass. Extended `.github/workflows/ci.yml` with a Playwright install + `npm run test:e2e` stage. **First real CI run after this only got through `typecheck` before hitting the same 3 pre-existing lint issues every T0.x item this session had logged as known debt** — but now that debt was blocking verification of the entire new CI pipeline, not just itself, so the calculus changed: fixed all 3 (React's officially-recommended "adjust state during render" pattern replacing `setState`-in-`useEffect` in `experiments-table.tsx`/`mobile-nav.tsx`; a ternary-for-side-effects in `test-embeddings.ts` became a plain if/else). Re-ran the full local Playwright suite after the refactor to confirm it was behavior-preserving (7/7 still passed) before pushing. **Final result: the CI run at commit `9241b64` completed fully green in 1m34s — install, typecheck, lint, unit tests, build, Playwright browser install, and the entire E2E+axe suite all passed on GitHub Actions itself**, not just locally. Lockfile regenerated clean and verified against npm 10.9.8 before every push (two new dep additions: `@playwright/test`, `@axe-core/playwright`). **Still deferred:** component tests, more E2E flows, Supabase RLS integration tests (needs Docker — available in CI but not attempted this pass, writing untestable-locally SQL felt too risky), migration-validation/dependency-audit/secret-scan CI stages, and branch protection on `master` (explicit confirmation needed first — a shared repo-settings change). **Not yet promoted to master/prod.**
- 2026-07-25 — **T0.6 phase 1+2 shipped on dev (Vitest unit tests + minimal CI)** — sixth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0, but only PARTIALLY complete (model: Sonnet 5). T0.6's full scope (Testing Library, Playwright E2E across ~10 flows, Supabase local RLS integration tests, axe, a 9-stage CI pipeline, branch protection) is much larger than any prior Tier 0 item — checked the environment first (no Docker on this dev machine, so `supabase start`/local RLS tests can't run here; no Playwright browsers cached yet; nothing test/CI-related existed at all) and asked the user how to scope this pass rather than attempting everything at once; chose "Phase 1+2 now": Vitest + a couple of real unit tests, plus a minimal CI workflow. Added `vitest` and real tests for `lib/schemas.ts` (the T0.1 Zod rules — first time those had an automated check instead of only manual browser verification) and `lib/search.ts`'s `parseQuery` (metal aliases, pH/m-z parsing, compound/method matching, and a regression-guard test documenting that "wet-dry cycling" resolves to `%cycling%` not `%wet%dry%` due to if/else ordering). Files with `import "server-only"` needed a stub alias (`tests/stubs/server-only.ts`) to load under plain Vitest, since that package is a real npm dependency Next resolves internally but isn't actually installed in `node_modules`. New `.github/workflows/ci.yml` runs install→typecheck→lint→unit→build on push/PR to `dev`/`master`; confirmed via a real (trap-guarded, restored) test that `next build` needs zero secrets to succeed, so CI needs no GitHub secrets configured. Lockfile regenerated clean and verified against npm 10.9.8 before pushing (per this session's earlier lockfile-failure lessons). **Verified the CI pipeline itself actually works** (not just "the YAML looks right"): pushed, confirmed via `gh run view` that it ran on GitHub Actions and correctly passed install/typecheck, then correctly stopped at lint — on the same 3 pre-existing, unrelated lint issues (`react-hooks/set-state-in-effect` in `experiments-table.tsx`/`mobile-nav.tsx`, an unused-expression warning in `test-embeddings.ts`) that every T0.x item this session had already noted and left untouched as known debt; deliberately did not fix them now either, to stay consistent and surgical — CI correctly surfacing pre-existing issues is the point of adding CI, not a regression. `npm test`/`npm run build` are separately confirmed green locally (lint blocks them from running in CI until fixed). **Deferred to a later pass:** component tests, Playwright, RLS tests, axe, remaining CI stages, branch protection. Not ticking the T0.6 checkbox until those ship.
- 2026-07-25 — **T0.5 shipped on dev (durable indexing job queue)** — fifth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0, spec-first per its `⚠` flag (model: Sonnet 5). Brainstormed the worker mechanism first via the [[ChemMemo_Feature_IndexJobs_Spec]] design doc (approved before code): an in-process `setInterval` poller was chosen over Railway cron or Supabase `pg_cron`/`pg_net`, since this app already runs as a single persistent Node process on Railway (same assumption T0.3's rate limiter relies on) — zero new infrastructure for a path meant to rarely fire. Migration `20260725120000_index_jobs.sql` adds an `index_jobs` table plus an `AFTER INSERT OR UPDATE` trigger on `experiments` (`enqueue_index_job`, matching the existing `record_experiment_revision` SECURITY DEFINER pattern) that upserts a `pending` job row **in the same transaction** as every experiment write — genuinely durable, not just "durable if the app remembers." One row per experiment (`ON CONFLICT` upsert, not append) so a re-edit resets the same job rather than piling up duplicates. New `lib/index-jobs.ts`'s `runIndexJob` is the single entry point both the unchanged fire-and-forget fast path (`new/actions.ts`, now calling `runIndexJob` instead of `syncExperimentEmbedding` directly) and the poller (started once via `instrumentation.ts`, Next's official server-startup hook) use — so a job's status (`done`/`pending`/`failed`, with `attempts`/`last_error`/`embedding_model`/`embedding_dimensions`) always reflects the true outcome, up to 5 backoff retries. Also fixed `lib/sync-embedding.ts` silently swallowing `experiment_embeddings` write errors (needed so job tracking can tell success from failure). Verified extensively given this touches a DB trigger firing on every experiment write: confirmed no other `UPDATE experiments` call sites exist beyond edit/soft-delete; tested the trigger in a rolled-back transaction on dev before trusting it; after deploy, confirmed via Railway runtime logs that `[index-jobs] poller started` fired; created a real throwaway experiment (`EXP-017`) via the UI and confirmed its `index_jobs` row reached `status: done` with `embedding_model: gemini-embedding-001`, `embedding_dimensions: 1536`; soft-deleted it and confirmed the trigger re-fired, re-ran the job, and correctly dropped the embedding row (`embedding_rows: 0`) while marking the job `done` again. Reindex-by-model-version tooling and the admin/health UI are explicitly deferred (T0.10) per the spec. **Not yet promoted to master/prod.**
- 2026-07-23 — **T0.4 shipped on dev (strict lab-only Ask mode)** — fourth item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5). `/api/ask` previously fell back to a general-knowledge answer whenever retrieval found nothing, mixed into the same UI as grounded answers behind only a small gray disclaimer — a real provenance risk. Now the client sends an explicit `mode: "lab" | "context"` (validated server-side, defaults to the safe `"lab"`); lab mode never calls `streamGeneralAnswer` — an empty retrieval returns "No matching experiments found in your lab" with no LLM call and no `ai_requests` row at all; context mode always answers from general knowledge (skips retrieval), rendered in a visually distinct amber-bordered card (reusing the existing `--amber` token) labelled "Scientific context — not from your lab's experiments." Mode toggle persists via `?mode=context` in the URL (`history.replaceState`, no full navigation) and is reflected in `ai_requests.endpoint`. Verified live on dev: a nonsense query in lab mode showed the explicit empty state with no fallback answer; "what is a coacervate" in context mode streamed the amber-labelled card and updated the URL; switching back to lab mode and asking "experiments with m/z 297" still returned a normal grounded, cited answer with source cards. **Not yet promoted to master/prod.**
- 2026-07-23 — **T0.3 shipped on dev (AI endpoint rate limits & telemetry)** — third item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5). New `lib/rate-limit.ts` (in-memory, single-process — see the plan's T0.3 tunables callout for exact numbers: 12 req/min/user, 1 concurrent/user, 5 concurrent global, 2000-char query cap, 8KB body cap on `/api/ask`). Migration `20260723130000_ai_requests.sql` adds a table logging every actual AI call (`ask_grounded`/`ask_general`/`summary_single`/`summary_group`) with model, source count, latency, a `chars/4` token estimate, and status — never prompt/answer text — via `createAdminClient()`, matching the `ai_summaries` write pattern; RLS read-own. `app/api/ask/route.ts`, `ask/actions.ts` (`generateGroupSummary`), and `summary-actions.ts` (`generateSummary`) all check rate+concurrency before calling the LLM; rate-limited attempts return 429 (`/api/ask`, client already shows a friendly retry message for any non-2xx) or `null` (the two summary actions, matching their existing AI-disabled no-op shape) without logging a row. Keyless search is untouched — it never reaches this code path. Verified on dev: asked a real question via the UI, got a normal streamed answer, then confirmed a matching `ai_requests` row (`ask_general`, `status: ok`, `model: gemini-flash-latest`, `latency_ms: 4956`, `est_tokens: 485`, no prompt text). Rate-limit/concurrency-rejection paths were **not** exercised live (would need rapid repeated calls; skipped as low-value for this pass, logic is straightforward and type-checked). **Not yet promoted to master/prod.**
- 2026-07-23 — **T0.2 shipped on dev (server-side file & URL hardening)** — second item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5). Migration `20260723120000_experiment_files_metadata.sql` adds `mime_type`/`byte_size`/`sha256`/`uploaded_by` to `experiment_files` (applied to dev via raw SQL + PostgREST reload, verified columns present). `file-actions.ts`: `uploadFile` now rejects oversized (>10MB) and disallowed-extension files server-side before touching storage, and hashes the upload (sha256) for the new metadata columns; `addFileLink` now requires `new URL()` to parse with `protocol === "https:"` and no embedded credentials — closes a **real, live stored-XSS hole**: a `javascript:` URL saved as a link was rendered directly as `<a href>` in `file-list.tsx` and would have executed in another lab member's session on click (not just theoretical audit language — confirmed by tracing the render path before fixing). `removeFile` reordered to delete the (RLS-gated) row first and only remove the storage object once that's confirmed, instead of the reverse (previously a non-owner's call was a misleading silent no-op returning `{ok:true}`, though RLS already blocked actual data loss). Verified via `claude-in-chrome` on a real owned experiment (EXP-013): `javascript:alert(1)` link rejected with a toast and nothing saved; a valid `https://example.com` link saved, appeared in the Files list, and was removed cleanly. **Upload-extension-rejection was not verified live** — browser automation's programmatic file-input assignment didn't trigger the app's `onChange`/submit (no console errors; a test-harness limitation, not an app bug), so that path is verified by code review + build only, by explicit user sign-off to skip. **Not yet promoted to master/prod.**
- 2026-07-23 — **T0.1 shipped on dev (shared Zod validation)** — first item of [[ChemMemo_Product_Evolution_Plan]] Tier 0 (model: Sonnet 5), commits `e90dbd7`/`9e32f96` (+ two lockfile-fix commits). New `lib/schemas.ts` (`experimentInputSchema`, `projectLabelSchema`); `ActionResult` extended with optional `fieldErrors`; `createExperiment`/`updateExperiment` validate via the shared schema and return field-level errors instead of `parseForm` silently coercing bad numbers to `null` or no-opping on an empty name; `createProject` gets the same length check. **Two real bugs found and fixed only because of the mandatory live browser smoke test (guardrail #8) — would have shipped broken otherwise:** (1) Railway's dev deploy FAILED twice on `npm ci` — adding `zod` left `package-lock.json` inconsistent for some platform-optional `@emnapi/*` transitive deps (`@tailwindcss/oxide-wasm32-wasi`'s peer deps), and my local `npm ci` (npm 11.6.2) didn't catch it because Railway runs npm 10.9.8, which is stricter; fixed by wiping `node_modules`+lockfile and reinstalling clean, then verifying with `npx -y -p npm@10.9.8 -- npm ci` before pushing. (2) First working deploy revealed that using `useActionState` + `<form action={fn}>` makes React 19 auto-reset ALL uncontrolled fields after any action call — including a failed validation — so a bad pH wiped the whole form, not just the invalid field; switched `ExperimentForm` to a manual `onSubmit` + `useTransition` calling the action directly, which doesn't trigger that reset. Also hit a third infra hiccup: the GitHub→Railway auto-deploy webhook silently didn't fire for ~11 min on one push; worked around with `railway up --detach --environment dev`. All three issues + workarounds logged in the plan's guardrails (§0.5) for future items. Verified end-to-end via `claude-in-chrome`: invalid pH/cycles rejected with inline errors + input preserved, valid save redirects normally, over-length project name rejected with a toast, all test records cleaned up. **Not yet promoted to master/prod** — dev-only per the plan's tier-by-tier gate.
- 2026-07-21 — **User-managed projects shipped** (dev + prod, commit `c47384d`, model: Sonnet 5). Brainstormed and wrote a design spec ([[ChemMemo_Feature_ProjectManagement_Spec]]) before implementing: projects were previously read-only reference data (4 hardcoded seed rows). Migration `20260721120000_projects_ownership.sql` adds `projects.owner_id` (nullable — seed rows stay ownerless), changes `experiments.project`'s FK to `ON DELETE SET NULL` (looked up the FK's actual name dynamically via `information_schema` rather than hardcoding it, since it was applied via raw `db query` on both dev and prod, not `db push` — dev's migration-tracking table doesn't line up with local filenames, a pre-existing mismatch from earlier MCP-applied migrations, left alone rather than "repaired" mid-feature), and adds `projects_insert_own`/`projects_delete_own` RLS policies (delete allowed for the owner **or** any ownerless row, so the seed projects could be cleaned up through the same path). New `app/(app)/projects-actions.ts`: `createProject` (slugifies the label into the id, retries on collision) and `deleteProject` (blocks with a friendly count-based error if active experiments still reference it). `sidebar-nav.tsx` gained a delete (×) per project row (shown when owned or ownerless) and an inline "+ New project" form (name + 6-swatch color picker). **Forgot to `NOTIFY pgrst, 'reload schema'` after the first raw-SQL migration apply** — PostgREST's cached schema didn't know about the new `owner_id` column yet, so `select *` omitted it and the owner-check silently evaluated false for every row (no delete/create UI appeared on dev until the reload). **Incident:** fixing an unrelated Railway branch mixup from the prior session (`railway service source connect`) turned out to not be environment-scoped despite the `--environment` flag — repointing dev's branch back to `dev` also silently reset production's branch to `dev` for a few minutes (caught before any broken deploy went live; reverted immediately). Switched to `railway up` (direct deploy bypassing the shared GitHub link) to ship to dev safely instead of touching that command again; Yishi then fixed both environments' branch mappings directly in the Railway dashboard. Verified full flow on dev (create → assign to a throwaway experiment → delete blocked with a friendly message → soft-delete the experiment → delete succeeds, confirming the `ON DELETE SET NULL` fix handles soft-deleted rows too) before shipping to prod and removing all 4 seed projects there (1 via the real UI, 3 via direct SQL after the browser session became unreliable — same verified end state).
- 2026-07-20 — **🚀 Sprint S6 shipped — ChemMemo is LIVE IN PRODUCTION** (commit `d5c7e88`, model: Sonnet 5). URL: https://chememowebapp-production.up.railway.app. Restored the paused prod Supabase project (`chememo`); applied all 8 migrations via `supabase db push` (Supabase MCP OAuth was broken this session — "Unrecognized client_id" — used the CLI instead); kept the `projects` seed and dropped the 12 demo experiments per Yishi's call (empty start for real lab use); set prod Auth Site/Redirect URLs + Railway prod env (Gemini provider/keys); merged `dev`→`master`, deployed. **Incident:** right after that first deploy, the Railway prod service got repointed to an unrelated repo (`MFP_analysis_Webapp`, a different Yishi project, deployed via a Cursor agent sharing the same Railway service) — prod 404'd with FastAPI-style errors; fixed with `railway service source connect --repo yishiezerzer-dot/chememo_webapp --branch master`, redeployed correct code immediately. **Full browser-driven prod smoke test:** signup/login, create experiment (→ `EXP-013`, confirming sequence continuity from dev), edit → History panel populated (#24), file upload → signed Storage URL opens, global search, sidebar project filter, upload toast, two-step soft-delete — all ✅. **Bug found + fixed during the smoke test:** Gemini's streaming fetch in `lib/llm.ts` (`chatStream`) had no timeout — when Gemini accepted the connection but never sent a chunk, the server's reader loop hung forever and Ask AI's UI showed a stuck blank cursor with no error; separately `ask-client.tsx`'s failure message was nested under a `meta`-truthy check so an outright-failed request showed nothing at all. Fixed with a 30s `AbortSignal.timeout` on the fetch + un-gating the failure message; verified on prod — a forced failure now shows "Something went wrong answering that. Please try again." instead of hanging. Shipped in a follow-up `dev`→`master` deploy. Gemini's `gemini-flash-latest` was intermittently 503'ing ("high demand") throughout the session, confirmed via Railway logs as external/transient — worth re-verifying AI features once Gemini's capacity normalizes, but not a ChemMemo bug. Deleted the smoke-test data (`EXP-013`) from prod afterward via the UI. **🎯 Every audit item + all 6 sprints + Phase 10 are now done. ChemMemo is fully shipped.**
- 2026-07-20 — **Bugfix: detail page crashed for an experiment's OWNER** (commit `f25e2c4`, model: Opus 4.8). Found during the owed dev check (Yishi created an experiment and couldn't open it). Root cause: `FileList` was handed a plain factory closure `(fileId) => removeFile.bind(null, fileId, e.id)` from the server component — RSC forbids passing non-action functions to client components ("Functions cannot be passed directly to Client Components"). **Latent, not from this session's features** — it only fires when `isOwner` is true, and the 12 seed experiments aren't owned by the viewer, so it never triggered until the first user-created experiment. Fix: pass the raw `removeFile` server action + `experimentId` prop; build the per-file click closure inside the (client) `FileList`. Build green, deployed. ⚠️ **This would also have hit production in S6** — good catch before promotion; re-verify "view own experiment" is in the S6 smoke test.
- 2026-07-20 — **Features #23 + #24 shipped on dev — audit backlog complete except S6** (model: Opus 4.8). **#23 Ask POST + streaming** (`836707d`): new `POST /api/ask` route handler — first line is JSON metadata (mode/grounded/sources), then the answer streams token-by-token; query is in the POST body (no URL logging). `lib/llm` gained `chatStream` (Gemini `streamGenerateContent?alt=sse`, thinkingBudget 0; openai/anthropic yield one full chunk) + `streamAnswer`/`streamGeneralAnswer`; `lib/rag` gained `retrieveRecords` (retrieval without generation). `ask/page.tsx` is now a shell; new `components/ask-client.tsx` posts, reads the stream, renders the answer incrementally (blinking caret) + sources + group summary + keyless view; removed old `ask-box.tsx`. Verified Gemini streaming yields text chunks via a throwaway probe before shipping. **#24 Edit history** (`99e1e2a`): migration `20260720120000_experiment_revisions.sql` — `experiment_revisions` table + AFTER UPDATE trigger `record_experiment_revision()` (SECURITY DEFINER, `editor_id = auth.uid()`, `to_jsonb(old)` snapshot; RLS read-all, trigger-only writes), **applied to chememo-dev only** (verified table/trigger/fn/policy present; trigger fire tested in a rolled-back tx so no data pollution). `lib/experiments listRevisions` + `ExperimentRevision` type; `components/history-panel.tsx` shows each edit newest-first with the changed fields (diff of consecutive snapshots) on the detail page. Both builds green, no dep changes. **🎯 The entire audit (P0–P3 + advanced) is now done except Sprint S6 (production promotion).**
- 2026-07-16 — **Features #20 + #21 shipped on dev** (commit `ac83f1e`, model: Opus 4.8). **#20** compound/metal autocomplete: `listVocab()` (lib/experiments) returns distinct compounds/metals; `ExperimentForm`'s `TagField` renders a native `<datalist>` of suggestions (threaded through `new/page.tsx`→`NewExperimentClient` and `edit/page.tsx`). Chose a **props+datalist** approach over the audit's per-keystroke server action — the vocab is tiny, so fetch once and let the browser filter (simpler, fewer round-trips). **#21** group summary: `summarizeGroup()` in `lib/llm.ts` (cited `[EXP-###]` set summary); `app/(app)/ask/actions.ts` `generateGroupSummary(ids)` reads the grounded result set via RLS; `components/group-summary.tsx` shows a "Summarise these N experiments" button on multi-result grounded asks (on-demand, **not cached** — dropped the audit's `ai_summaries` group caching to avoid a keying scheme for an ephemeral, query-tied summary). No dep/lockfile change; build green. **Remaining before S6:** P3 #23 (Ask POST/streaming) and #24 (edit-history table — needs a dev migration, same approval gate as S2).
- 2026-07-16 — **Features #11 + #22 shipped on dev** (commit `0e44aff`, model: Opus 4.8). **#11** dashboard "Recent activity" panel — last 8 experiments by `updated_at`, relative timestamps (`timeAgo` helper), each linking to detail, reusing the mockup's `.act-row`/`.act-dot`/`.ai-label`/`.at` CSS. **#22** "Export CSV" button in the experiments toolbar downloads the currently-filtered `rows`; **hand-rolled CSV serializer** (`csvCell`/`toCsv`, RFC-style quoting, arrays joined with "; ", CRLF) — deliberately **no `papaparse`/new dependency** (the audit suggested papaparse, but per the #15 lesson we avoid dep changes; a client-side Blob download needs none). No lockfile change → clean deploy. Build green. **Remaining before S6:** P3 #20 autocomplete, #21 group summary, #23 Ask streaming, #24 edit history.
- 2026-07-16 — **P2 tech-debt cluster: #14 + #13 shipped, #15 reverted, #16 done** (model: Opus 4.8). #16 README (`7bdb9f3`). Then #14/#15/#13 in `31c8ebe`: **#14** fonts self-hosted via `next/font/google` (Fraunces/Sora/JetBrains Mono; dropped the external Google Fonts `<link>`; `chemmemo.css --display/--ui/--mono` point at the font vars); **#13** renamed `lib/anthropic.ts`→`lib/llm.ts` (it dispatches all providers) + repointed 5 importers; **#15** removed unused Tailwind. **#15 broke the Railway deploy** — dropping Tailwind churned `package-lock.json` around its native `@emnapi`/`oxide` deps and Railway's `npm ci` rejected the out-of-sync lock (FAILED twice; build succeeded locally but `npm ci` is stricter than the local `npm install`). **Fix: reverted #15 only** (restored package.json/lock/globals.css/postcss to the known-good state via `git show 31c8ebe~1`, revert commit `574525e`), keeping #14/#13. Tailwind stays as unused-but-harmless. Local `npm ci` also hit an unrelated Windows EPERM file-lock on a lightningcss `.node` binary, so local build was verified via `npm install`+build (green); the restored lock is byte-identical to the one that deployed successfully at `4389bbe`. **Lesson: don't remove deps with native/optional binaries late in a session without a clean `npm ci` validation before pushing.** P2 now fully closed (#13/#14/#16/#17/#18 done, #15 won't-do). **Remaining before S6:** P1 #11 (activity feed); P3 #20–#24.
- 2026-07-16 — **Pre-prod backlog batch #12/#17/#18 shipped on dev** (commit `1ef18f3`, model: Opus 4.8). User decided to finish ALL remaining audit items before production (no rush on prod). Started with the recommended de-risk batch: **#17** — `ilikeCond()` in `lib/search.ts` double-quotes free-text `.or()` ilike values so `%`, `,`, `(`, `)` are treated as literals, not PostgREST or()-syntax (real latent bug on the AI-router path, e.g. a term like "poly(A)" would break the filter parse; the keyless tokenizer never emits those chars so it was latent). **#18** — `updateExperiment` now drops the cached single-scope `ai_summaries` row (best-effort service-role delete) so an edited experiment shows "regenerate" instead of a stale AI summary. **#12** — dashboard's fake "100% Cited results" stat replaced with "Logged this month" (counts `experiments.created_at` in the current calendar month). Build clean; deploy SUCCESS. **Remaining before prod:** P1 #11 (activity feed); P2 #13–#16 (rename anthropic→llm, next/font, Tailwind decision, README); P3 #20–#24 (autocomplete, group summary, CSV export, Ask streaming, edit history). Then S6 (prod promotion). Owed manual check: create an experiment on dev → confirm a fresh `experiment_embeddings` row.
- 2026-07-16 — **Sprints S3–S5 (audit P1/P2) shipped on dev** (commit `910b2e4`, model: Opus 4.8). User: finish everything that doesn't need prod, leave S6 (prod promotion) for last. **S3** (dynamic sidebar + URL filter parity): `SidebarNav` now renders real `listProjects()` rows linking to `/experiments?project=<id>` (was 4 hardcoded dead links); `layout.tsx` fetches projects; `ExperimentsTable` gained `useEffect`s to re-sync `q`/`project` state from the URL on navigation (so clicking a sidebar project while already on the page re-filters). **S4** (feedback): `components/toast-provider.tsx` (React context + `useToast`, reuses the existing `.toast`/`.toast.show` CSS) wraps the app in `layout.tsx`; file `uploadFile`/`addFileLink`/`removeFile` actions now return a shared `ActionResult` (`lib/types.ts`) instead of throwing, and `FileManager`/`FileList` toast success/error (upload path is the audit's acceptance criterion); added `app/(app)/error.tsx` (retry boundary — no more blank page on a thrown Server Component error), `app/(app)/loading.tsx` (glass skeleton), `app/(app)/ask/loading.tsx` (AI spinner). Left save/AI-gen toasts alone (they redirect / already show inline state). **S5** (retrieval eval): `eval/retrieval-queries.json` (10 queries, honest expected IDs from the real 12 seeds — 6 semantic + 4 deterministic-filter) + self-contained `scripts/eval-retrieval.ts` (`npm run eval:retrieval`, mirrors the backfill's @/-free import pattern so it runs under plain node), computes precision/recall@k, **exits 1 if any query recall < 0.8**. **Verified on dev: 100% recall on all 10 queries, mean precision 56%** (semantic retrieves top-8 so precision is naturally low; recall is the gate). Build clean; deploy SUCCESS. **Only remaining audit P0 is Sprint S6 (prod promotion) — deferred to the end by the user, needs explicit go-ahead.**
- 2026-07-16 — **Sprint S2 (audit P0) shipped on dev** (commit `a65bcbb`, model: Opus 4.8). Implemented audit P0 #3 and #5. (1) **Atomic EXP-ID sequence**: migration `20260716120000_experiment_id_sequence.sql` creates `experiment_id_seq` (seeded just past the current max → START 13) + a `next_experiment_id()` SQL function granted to `authenticated`; `lib/experiment-id.ts` calls it by RPC; removed the racy read-max-plus-one generator from `new/actions.ts`. Migration **applied to chememo-dev only** (`khkpqnpmhravdpbogqai`) — verified seq start=13, unused, fn present. Prod (`iazuubcyxneavrahjgww`) deliberately untouched (that's Sprint S6). (2) **Embedding auto-sync on save**: `lib/sync-embedding.ts` (service-role) re-embeds an experiment on create/update and deletes its `experiment_embeddings` row on soft-delete; wired **fire-and-forget** (`void …catch(log)`) into `createExperiment`/`updateExperiment`/`softDeleteExperiment` so a slow embed API never blocks the redirect (Railway's persistent server finishes it in the background). No-ops without a key. Build clean; deploy SUCCESS; dev routes 307 (new modules load, no runtime crash). ⚠️ **Applying the migration was auto-mode-blocked** as a "production deploy" and required explicit user approval — same guardrail expected for Sprint S6. **Manual verification still owed**: log in on dev, create an experiment, confirm a fresh `experiment_embeddings` row appears (acceptance criterion). **Remaining P0: only Sprint S6 (prod promotion — needs go-ahead).**
- 2026-07-16 — **Sprint S1 (audit P0) shipped on dev** (commit `d159b6c`, model: Sonnet 5). Implemented audit P0 #1 and #2: **mobile navigation** (`components/mobile-nav.tsx` — hamburger toggles the previously-inert `.sidebar.open` CSS drawer, backdrop click, Escape, closes on route change) and **functional global search** (`components/global-search.tsx` — topbar input now navigates to `/experiments?q=…`, was decorative). Also wired URL-param pre-filtering: `experiments/page.tsx` reads `searchParams` (`q`, `project`) and seeds `ExperimentsTable`'s state, so filters are shareable/bookmarkable links. Build clean; route smoke-test confirmed no crashes with the new params. **Remaining P0 from the audit:** embedding-sync-on-save + EXP-ID sequence (Sprint S2 — user deferred, needs a stronger model for the RLS/concurrency subtlety) and **prod promotion** (Sprint S6 — flagged for confirmation before running, since it's a production deploy of the kind auto-mode blocked earlier this session). See [[ChemMemo_Audit_Roadmap]] §4/§5 for the full sprint sequence.
- 2026-07-15 — **Audit added to vault.** Reviewed the comprehensive audit ([[ChemMemo_Audit_Roadmap]]), verified its claims against the `dev` code (Next 16.2.10 / React 19.2.4, no error/loading/not-found routes, Tailwind imported but unused, README still boilerplate — all accurate; the `next/dist/docs` path it cites does exist), and improved it: added an **executive summary (§0)** with the top-5 priorities, flagged the already-shipped Ask loading spinner + dynamic general answers, sharpened the fragile grounded-check recommendation (→ structured output), corrected the similarity-threshold note (env-tunable `SEMANTIC_MIN_SIMILARITY`), generalized the "Sonnet" framing to any coding agent, and added Obsidian frontmatter + `[[wikilinks]]`. Linked from the hub's Project notes. **Its P0 list (embedding-sync-on-save, mobile nav, EXP-ID sequence, prod promotion) is the authoritative backlog for post-build work** — supersedes the looser "next actions" where they overlap.
- 2026-07-15 — **Ask UX + dynamic answers** (commit `a626184`). (1) **Loading state**: `components/ask-box.tsx` — client query box + example chips using `useTransition`; shows a spinning "Thinking…" card while the server does embedding+retrieval+LLM (the page was a server GET form with no feedback before). (2) **Dynamic general answers**: when a question doesn't match any experiment, the AI now still answers from general chemistry knowledge, clearly labelled "General answer — not based on your lab's experiments" (no citations/sources); grounded answers keep `[EXP-###]` + Sources. Mechanism: `semanticSearch` keeps only hits ≥ **0.5** cosine (`SEMANTIC_MIN_SIMILARITY`, tunable; probed on/off-topic = 0.59–0.70 vs 0.41–0.43), and if grounding yields the "no matching" guardrail it falls back to `generateGeneralAnswer`. `AskResult` gained `grounded:boolean`. Verified routing: droplets→grounded(0.68), France/photosynthesis→general(0.41/0.46). Also: plan backed up into the app repo at `docs/` (commit `9e801d1`). ⚠️ docs/ copy is a point-in-time snapshot — re-copy from the vault when refreshing the repo backup.
- 2026-07-15 — **Phase 10 activated via Gemini** (commit `67ab912`). Yishi provided a free **Gemini** API key; wired a **switchable provider layer** (`AI_PROVIDER=gemini|openai|anthropic`) keeping all three backends. Chat: `lib/anthropic.ts` `chatComplete` → Gemini native `generateContent` (**thinkingBudget 0** — 2.5/3.x are thinking models, else they burn the output budget on hidden reasoning and return empty), or OpenAI/Anthropic SDK. Embeddings: `lib/embeddings.ts` → Gemini `embedContent` @ `outputDimensionality 1536` (or OpenAI `dimensions 1536`) — matches `vector(1536)`, **no migration**. Key facts learned: key auth is **query-param `?key=`** (not Bearer); OpenAI-compat chat endpoint 404'd `gemini-2.5-flash` for new users → used **native REST + `gemini-flash-latest`**; free-tier rate limits are real (throttled backfill 600 ms). **Verified end-to-end on dev**: 12/12 embeddings; semantic "droplets/coacervate" → EXP-006/012/004; grounded answer cites [EXP-004/006/012], no hallucination. Key in `.env.local` (gitignored) + Railway dev env only — **not committed**. Cleaned stray test experiments (EXPF/RLS/SD/PROBE/STORTEST) → back to exactly 12 seeds + 12 embeddings. **Pending: retrieval eval set; prod promotion.**
- 2026-07-14 — **Phase 9 complete (code only, inert)** (commit `e59d221`). LLM-assisted entry: `extractExperimentFields` in `lib/anthropic.ts` (Claude parses messy notes → structured-fields JSON, only stated fields, type-coerced, methods restricted to known set; null without key). `extractFromNotes` auth-gated server action. `PasteNotes` box (disabled + Phase-10 note without key) → `NewExperimentClient` sets extracted fields as form `initial` and remounts `ExperimentForm` (keyed) pre-filled; **never auto-saves** — user reviews + clicks Save. `ExperimentForm.initial` relaxed to `Partial<Experiment>`. Build clean, no migration. **⭐ All build phases 0–9 DONE.** The whole app + every AI feature (embeddings, hybrid RAG, summaries, assisted entry) is written and inert, keyless MVP live on dev. **Next: Phase 10 — the finale: obtain Anthropic + OpenAI keys, add to `.env.local` + Railway, run embedding backfill, activate + verify all 🔑 features, eval set, final deploy. (Also still open: promote dev→prod.)**
- 2026-07-14 — **Phase 8 complete (code only, inert)** (commit `da7b1e0`). `summarizeExperiment` in `lib/anthropic.ts` (grounded, 2–3 sentences, never-invent, null without key). `generateSummary(id)` server action (`app/(app)/experiments/[id]/summary-actions.ts`): auth-gated, no-ops without key; else summarize + cache into `ai_summaries` (scope `single`, model=`claude-sonnet-5`, `source_ids=[id]`, timestamp), regenerate deletes+reinserts. Writes via new **service-role** client `lib/supabase/admin.ts` (trusted server-only; `ai_summaries` has no client-write RLS policy so admin write avoids a policy migration). `getExperimentSummary` reads latest cached row (session client, RLS read ok). `SummaryCard` on detail page: shows cached summary + model/date; Generate/Regenerate button only when `isLlmEnabled()`, else Phase-10 note. Build clean; inert (no rows, button hidden). **Group summary deferred** (good-to-have). No migration. **Next: Phase 9 (LLM-assisted entry, code only) — then Phase 10 activates all AI.**
- 2026-07-14 — **Phase 7 complete (code only, inert)** (commit `4c899f2`). `lib/anthropic.ts`: `isLlmEnabled` guard (ANTHROPIC_API_KEY), `routeQuery` (Claude → retrieval-plan JSON), `generateAnswer` (grounded, `[EXP-###]` citations, "no records" guardrail, never-invent) — model `claude-sonnet-5`, both no-op/null without key. `lib/rag.ts` `askAI`: inert → keyless (Phase 5) fallback; enabled → route + `executeFilters` (structured) + `semanticSearch` (`embedText`→`match_experiments`→hydrate) merged/deduped → `generateAnswer`. Refactored `search.ts` to export shared `executeFilters`. Ask screen now flows through `askAI`, renders grounded answer with linked `[EXP-###]` when present, else unchanged keyless view. Build clean; **keyless fallback regression-verified** (pH>8 cycling → EXP-004/009 through the shared path). No migration/DB change (uses existing `match_experiments`). Dev DB was already ACTIVE (restored 07-13). **Next: Phase 8 (AI summaries, code only) or Phase 9 (LLM-assisted entry).**
- 2026-07-13 — **Phase 6 complete (code only, inert)** (commit `2681f08`). `lib/embeddings.ts`: `buildEmbeddingInput` (deterministic), `isEmbeddingEnabled` guard, `embedText`/`embedExperiment` vs `text-embedding-3-small` — all **no-op returning null with no OPENAI_API_KEY**. `match_experiments(vector(1536), match_count)` cosine-distance fn (SECURITY INVOKER → RLS applies, non-deleted only) + hnsw index applied to `chememo-dev`; verified: fn+index exist, `match_experiments` runs and returns 0 rows (correct — 0 embeddings until Phase 10). `scripts/backfill-embeddings.ts` ready-but-not-run (inert path verified). **8/8** builder+guard tests pass (`scripts/test-embeddings.ts`, run via Node 24 type-stripping); `scripts/` excluded from app tsconfig (node-only `.ts` import extensions). ⚠️ **Op note:** free-tier `chememo-dev` **auto-paused after ~8 days idle** — had to `restore_project` (took a few min) before the migration would apply; expect this whenever returning after a gap. Prod `match_experiments` deferred to promotion. **Next: Phase 7 (Ask AI — hybrid RAG, code only).**
- 2026-07-05 — **Phase 5 complete on dev — keyless MVP** (commit `cc596fd`). `lib/search.ts`: deterministic parser → exact Postgres filters (compounds `contains`, metals/methods/mz `overlaps`, pH comparator via gt/lt/gte/lte/eq, reaction `ilike`, free-text `or` over observations/name/notes), each result cited `EXP-###`, shows an "interpreted as" panel + Sources list. **Ask screen** (`app/(app)/ask`), real **dashboard** (hero + stat counters + recent). Structured filters **verified exact** on dev: His+TGA+Zn→001/004/008, m/z 297→001/004/008/009/011, wet-dry cycling pH>8→004/009, depsi+NMR→002/006/010. Free-text is deterministic **keyword** search — honestly returns "no droplets" experiments too (negation is the Phase-7 AI job); UI labels it "text mentions …". Fixed a TS error (dynamic `q[op]()` → explicit gt/lt/gte/lte/eq switch). **This is the shippable keyless MVP (Phases 0–5): auth + typed records + CRUD + files + exact cited search, zero API cost.** ⚠️ Still on `dev` only — promotion to `master`/prod (apply all migrations incl. storage to `chememo` prod, set prod Supabase Auth URL config) is a deliberate separate step. **Next: promote to prod when ready, or build inert AI code (Phases 6–9).**
- 2026-07-05 — **Phase 4 complete on dev** (commit `c68dc0a`). Private Supabase Storage bucket `experiment-files` (10 MB) with `storage.objects` RLS keyed to the experiment id in the object path (`<experiment_id>/<file>`): read if parent experiment readable, insert/update/delete if you own it. Server actions (`app/(app)/experiments/[id]/file-actions.ts`): `uploadFile` (storage + `experiment_files` row, rolls back the object if the row insert fails), `addFileLink` (external OneDrive/Drive URL), `removeFile` (deletes object + row). Detail page resolves **signed URLs** for uploads (private bucket) so files open in a new tab and survive a fresh session; owner-only `FileManager` (upload input auto-submits; add-link form) + two-step remove; non-owners see a read-only list. **Verified 10/10** on dev. **Bug found + fixed:** first storage policies put `storage.foldername(name)` inside `select 1 from experiments e`, where unqualified `name` bound to `experiments.name` (the title) not the object path → every upload denied by RLS; rewrote as `(storage.foldername(name))[1] in (select e.id …)` (path evaluated at outer scope). Original migration file also corrected so a fresh prod apply is right. **Next: Phase 5 (keyless search + ship MVP).**
- 2026-07-05 — **Phase 3 complete on dev** (commit `7dbea3f`). Experiment CRUD wired to Supabase (`chememo-dev`): `lib/types.ts` + `lib/experiments.ts` (server data access, `deleted_at` filtered). Experiments **table** = live client search + sortable headers + project/pH filter chips over the 12 seeds. Shared **New/Edit form** (`components/experiment-form.tsx`) with compound/metal tag inputs, method multi-select, typed pH/cycles/mz; server actions in `app/(app)/new/actions.ts` insert/update with `owner_id`, new IDs continue `EXP-###`. **Detail** page: spec grid, m/z chips, linked files, owner-only Edit + two-step soft-delete (RLS-enforced). Build clean; **9/9 CRUD+RLS checks** on dev (create typed row → types preserved → pH>8 filter → edit persists → B can't edit A → soft-delete hides). *Deferred (noted good-to-haves):* compound/metal autocomplete still free-text; method-filter chip (search covers it); live form summary trimmed. **Next: Phase 4 (file handling) or Phase 5 (keyless search + ship MVP).**
- 2026-07-02 — **Phase 2 complete on dev** (commit `3fe7f48`). Schema-as-code in `supabase/migrations/`: `init_schema` (pgvector + 6 tables + `set_updated_at`/`handle_new_user` triggers + RLS), `seed_reference_and_experiments` (4 projects, EXP-001…012, 34 files from mockup `data.js`), `fix_soft_delete_rls`. Applied to **chememo-dev** via Supabase MCP. **RLS verified 10/10** with two real users (read-all; edit-own; owner-spoof blocked; B can't read/edit A's soft-deleted row; A sees own soft-deleted; profiles trigger fires). Deterministic `pH>8` filter returns EXP-004+EXP-009 (Phase-5 groundwork). **Bug found + fixed:** soft-delete was self-blocking — SELECT policy `deleted_at is null` made a just-deleted row fail row-security on its own UPDATE (`new row violates RLS`, reproduced in pure SQL); fix broadens read to `deleted_at is null OR owner_id = auth.uid()` (others still can't see deleted rows; app list queries still filter `deleted_at is null`). **Duplicate-signup UX** (Yishi's ask): Supabase hides "email exists" to prevent enumeration → returns a user with `identities: []`, no error, no session; `/login` now detects empty-identities and shows "account already exists — sign in" instead of a misleading confirm notice. ⚠️ **Prod schema deferred** — auto-mode correctly blocked applying migrations to `chememo` (prod); do it at promotion time (`master` merge) with approval. **Next: Phase 3 (Experiment CRUD UI).**
- 2026-07-02 — **Bugfix** (commit `4da2770` on `dev`): Yishi hit "confirm link didn't work / sign-out errored" on the dev site. Root cause: behind Railway's proxy `request.url` reports the container-internal host (`localhost:8080`), so absolute redirects built from it (signout → login, auth callback → dashboard) sent the browser to the user's own machine; the confirm link *did* verify the account (hence sign-in worked). Fixed both routes to use **relative `Location` headers**. ⚠️ Still pending (dashboard-only, needs Yishi): Supabase **URL Configuration** on `chememo-dev` — set Site URL to `https://chememowebapp-dev.up.railway.app` and add `https://chememowebapp-dev.up.railway.app/auth/callback` (+ `http://localhost:3000/**`) to Redirect URLs, else confirmation emails still redirect to localhost:3000. Same change needed on `chememo` (prod) with the production URL before Phase 5 ships.
- 2026-07-02 — **Phase 1 complete** (commit `bd54dc0` on `dev`). Supabase email/password auth via `@supabase/ssr`: `lib/supabase/{client,server,middleware}.ts` + root `middleware.ts` (session refresh + gating). Mockup ported: full `chemmemo.css` + bg assets carried over; `/login` (login/signup toggle, PKCE `/auth/callback`, `/auth/signout`); gated `(app)` shell — sidebar nav (Dashboard/Experiments/New/Ask AI + project links), topbar, theme toggle, user pill w/ initials + sign-out; placeholder pages for Phases 3/5. **Verified headlessly:** `npm run build` clean; test user `test.user@chememo-lab.dev` created (admin-confirmed) on `chememo-dev`, password sign-in returns valid session + metadata; `/`+`/dashboard` → 307 `/login`, `/login` → 200. Local `.env.local` now points at `chememo-dev`. *Notes:* real signups still require email confirmation (autoconfirm not enabled — Management API token unavailable from CLI keyring); Next.js warns `middleware` convention → rename to `proxy` someday; recommend a quick browser smoke-test of login/logout on the dev URL. **Next: Phase 2 (schema + RLS as supabase migrations).**
- 2026-07-02 — **Phase 0.5 complete.** `dev` branch pushed (`master`=production, `dev`=WIP); `supabase init` committed (Phase 2 schema goes in `supabase/migrations/*.sql`). Yishi deleted `platepost` (freed the 2-project limit) and installed the Railway GitHub App. Then: created Supabase project **`chememo-dev`** (ref `khkpqnpmhravdpbogqai`, eu-central-1, $0) and repointed the Railway **dev** environment's 3 Supabase vars at it (keys set directly, never written to disk/chat — re-fetch anytime with `supabase projects api-keys --project-ref khkpqnpmhravdpbogqai`). **Railway↔GitHub auto-deploy verified end-to-end:** push to `dev` auto-built + deployed (SUCCESS), push to `master` auto-deployed production (SUCCESS). Pipeline is now: commit → push → auto-deploy per branch. ⚠️ Local `.env.local` still points at the *production* Supabase — switch it to `chememo-dev` values when Phase 1 dev work starts.
- 2026-07-02 — Railway now has two environments in project `chememo_webapp`: **production** (https://chememowebapp-production.up.railway.app) and **dev** (https://chememowebapp-dev.up.railway.app), each with its own variables/deploys (dev duplicated from production, Supabase vars copied). Workflow: `railway up -e dev -s chememo_webapp` for WIP, promote with `-e production` when good. Decided **against** splitting the repo into frontend/backend folders — Supabase *is* the backend; a lone Next.js app is the professional norm for this stack. ⚠️ Both envs share the one Supabase project for now; consider a second free Supabase project for dev once real data exists.
- 2026-07-02 — **Phase 0 complete.** Supabase project `chememo` created (ref `iazuubcyxneavrahjgww`, eu-central-1, free tier). App scaffolded at `C:\dev\chememo_webapp` (create-next-app: TS, App Router, Tailwind, ESLint) — placed outside OneDrive because the course-folder path (apostrophe + Hebrew) breaks Node tooling. `output: 'standalone'` set; deps added (`@supabase/supabase-js`, `@supabase/ssr`, inert `@anthropic-ai/sdk` + `openai`); `.env.local` holds Supabase URL + anon + service_role (gitignored). GitHub: private repo `yishiezerzer-dot/chememo_webapp` (branch `master`). Railway: project `chememo_webapp` deployed with Supabase env vars → **https://chememowebapp-production.up.railway.app** (first build failed on `npm ci` lockfile drift — fixed by regenerating `package-lock.json`). Verify passed: local dev HTTP 200 + env vars load server-side; Railway URL HTTP 200. **Next: Phase 1 (Auth + app shell).**
- 2026-07-02 — Switched deploy target from Vercel to **Railway** (installed Railway CLI 5.23.3, `use-railway` skill, authenticated as Yishi Ezerzer). Updated Stack + Phases 0/5/10 accordingly; portability guardrails now list Render/Fly as fallback hosts instead of Vercel.
- 2026-07-01 — Restructured so AI API keys are the final step (Phase 10). Confirmed CLIs (node/npm/git/gh/supabase present; installed vercel). Supabase MCP connected. **Next: Phase 0.**
- 2026-07-01 — Plan authored. Decisions locked (Next.js + Supabase + Claude/OpenAI, hybrid retrieval, upload-small/link-big). No code yet.

---

## Phase 0 — Foundations & accounts
Goal: everything installed, accounts created, a blank Next.js app deploys.
- [x] Create **Supabase** project; copy Project URL, `anon` key, `service_role` key into [[ChemMemo]] secrets checklist (locally, not in git). *(→ `chememo`, ref `iazuubcyxneavrahjgww`; keys live in `C:\dev\chememo_webapp\.env.local`)*
- [x] 🔑 **AI API keys are NOT obtained now** — deferred to Phase 10. Skip Anthropic/OpenAI for the whole build until then.
- [x] Scaffold app: `npx create-next-app@latest chememo_webapp` (TypeScript, App Router, Tailwind, ESLint). *(→ `C:\dev\chememo_webapp`)*
- [x] **Portability guardrails (keep host-swap an afternoon, not a migration):** (1) set `output: 'standalone'` in `next.config.js` so a Dockerfile / `next start` on Railway (or Render/Fly as a fallback) is trivial; (2) **no host-proprietary storage or cron** — all data/auth/storage/vectors stay in Supabase; if scheduling is needed later use Supabase `pg_cron` or a GitHub Action, and keep API routes on the Node runtime. Rationale: state lives in Supabase, so only the stateless Next.js app ever moves. See [[ChemMemo]] decisions.
- [x] Add deps: `@supabase/supabase-js @supabase/ssr` now. (`@anthropic-ai/sdk openai` can be added now as deps but stay unused until Phase 10.)
- [x] Create `.env.local` with Supabase values only; add `.env*` to `.gitignore`.
- [x] Init git repo; push to GitHub; create a **Railway** project from the repo (`railway init` / `railway up` or link the GitHub repo in the Railway dashboard); add the Supabase env vars in Railway. *(→ private repo `yishiezerzer-dot/chememo_webapp`; Railway project `chememo_webapp`, live at https://chememowebapp-production.up.railway.app)*
- **Verify:** `npm run dev` shows the app locally **and** the Railway URL serves the default page. Supabase env vars load server-side.

## Phase 1 — Auth + app shell
Goal: real login gates a real app shell; the mockup's navigation works in React.
- [x] Supabase Auth: email/password (magic-link optional later).
- [x] Supabase browser + server clients (`lib/supabase/client.ts` + `lib/supabase/server.ts` via `@supabase/ssr`).
- [x] Port mockup **Auth** screen (login/signup toggle) to a real `/login` route.
- [x] Middleware / server checks: unauthenticated users are redirected to `/login`; authenticated users reach `/dashboard`.
- [x] App shell + nav (Dashboard / Experiments / New / Ask AI) ported from mockup; theme toggle carried over.
- **Verify:** sign up a test user → confirm email flow → log in → land on dashboard → refresh keeps session → log out returns to `/login`. Direct-URL to a gated page while logged out redirects.

## Phase 2 — Data model & DB (schema + RLS)
Goal: typed schema, security, seed data. **See "Data model" appendix below for exact SQL.**
- [x] Enable `pgvector` extension (so the embeddings column exists; it just won't be populated until Phase 10).
- [x] Create tables: `profiles`, `projects`, `experiments`, `experiment_files`, `experiment_embeddings`, `ai_summaries` (see appendix).
- [x] Typed/unit-aware columns (numeric pH, temp, cycles; arrays for compounds/metals/methods/mz); `owner_id`, `created_at`, `updated_at`, `deleted_at` (soft delete).
- [x] `updated_at` trigger; `profiles` auto-row trigger on new auth user.
- [x] **RLS**: authenticated users **read all** non-deleted rows (lab-shared); **insert/update/delete only own** (`owner_id = auth.uid()`). *(read policy also lets an owner see their own soft-deleted rows — required so the soft-delete UPDATE itself passes row-security.)*
- [x] Seed from the mockup `data.js` (EXP-001…EXP-012) so there's real content to test search. *(4 projects, 12 experiments, 34 files)*
- **Verify:** rows visible in Supabase Table Editor; as user A you can read B's rows but **cannot** edit them (RLS denies); soft-deleted rows hidden from normal queries.

## Phase 3 — Experiment CRUD UI (port the mockup)
Goal: create / list / view experiments against Supabase.
- [x] **Experiments table** page: fetch from Supabase; client search + sortable headers + filter chips (project / pH). *(method-filter chip deferred; search already matches method text)*
- [x] **New Experiment** form → typed insert; tag inputs (compounds/metals), method multi-select. *(live summary preview trimmed to a save panel — can revisit)*
- [x] **Experiment detail** page: full record, m/z chips, linked-file rows.
- [ ] Compound/metal **autocomplete** from existing values (good-to-have; stub now, fill later). *(still a stub — free-text tag input; deferred)*
- [x] Edit + soft-delete own experiments. *(owner-only, RLS-enforced; two-step confirm delete)*
- **Verify:** create a new experiment in the UI → it appears in the table → open detail shows all fields correctly → filter `pH > 8` returns the right rows → editing persists after refresh.

## Phase 4 — File handling (upload small, link big)
Goal: attach evidence to experiments realistically.
- [x] Supabase **Storage** bucket (`experiment-files`, private, 10 MB) with RLS aligned to experiments (path `<experiment_id>/<file>`; read if parent readable, write/delete if owner).
- [x] Upload path for images/spectra screenshots (private bucket → short-lived **signed URLs** to open; regenerated each render).
- [x] External-link rows for big LC-MS folders / Excel (OneDrive/Drive URL + label + type icon).
- [x] `experiment_files` rows tie both kinds to an experiment. *(upload rollback if row insert fails; owner-only remove deletes storage object + row)*
- **Verify:** upload a microscopy image → shows on detail page and reopens from a fresh session; add a OneDrive link → clicking opens it; access respects login.

## Phase 5 — Keyless search + deploy the MVP
Goal: a genuinely useful notebook **with zero AI keys**, shipped.
- [x] **Structured / exact search UI** ("Ask" screen, keyless mode): `lib/search.ts` parses NL-ish query → deterministic Postgres filters (compounds `contains`, metals/methods/mz `overlaps`, pH comparator, reaction `ilike`, free-text `or`). Fully cited with `EXP-###` + Sources list. Structured filters exact & verified; free-text is keyword-only and honestly labelled "text mentions …" (negation e.g. "no droplets" → Phase 7 semantic/AI).
- [x] UX polish: empty / no-filter / no-match states, interpreted-as panel, example chips, theme + responsive carried from mockup CSS. *(dedicated loading.tsx spinner not added — SSR is fast; could add later)*
- [x] Deploy to Railway (dev, auto-deploy from `dev`); smoke-test loop verified. *(prod deploy = promotion step, see below)*
- **Verify:** a fresh account can do the full keyless loop on the live URL. The lab could start using this today, no API cost. This is the shippable keyless MVP.

## Phase 6 — Embeddings pipeline 🔑 (code only; activates in Phase 10)
Goal: write the semantic-search plumbing now; it stays inert until keys exist.
- [x] Build the embedding **input string** builder (name + reaction + compounds + metals + methods + observations + notes) — `buildEmbeddingInput` in `lib/embeddings.ts`, pure + deterministic, unit-tested.
- [x] Write the embed call against OpenAI `text-embedding-3-small` behind a guard (`isEmbeddingEnabled` → `!!OPENAI_API_KEY`) that **no-ops (returns null)** when no key is set.
- [x] `match_experiments(query_embedding vector(1536), match_count)` SQL function (cosine distance `<=>`, SECURITY INVOKER so RLS applies) over non-deleted rows, + hnsw index. Applied to `chememo-dev`.
- [x] Backfill **script** ready to run (but not run): `scripts/backfill-embeddings.ts` — inert path prints a Phase-10 notice and exits.
- **Verify (deferred to Phase 10):** with a key, backfill populates vectors and `match_experiments` returns sensible neighbors for *"samples that formed droplets"*. Until then: unit-test the input-string builder and confirm the guard no-ops cleanly with no key.

## Phase 7 — Ask AI — hybrid RAG 🔑 (code only; activates in Phase 10)
Goal: write the full RAG path; the keyless filter path (Phase 5) keeps working meanwhile.
- [x] **Router**: `routeQuery` in `lib/anthropic.ts` — guarded Claude call → intent JSON `{mode, filters, semanticQuery}`; returns null (→ keyless fallback) when no key or unparseable.
- [x] **Structured path**: `executeFilters(filters)` extracted from `search.ts`, shared by keyless + AI; parameterized PostgREST operators (contains/overlaps/comparator/ilike), never string-concat SQL.
- [x] **Semantic path**: `semanticSearch` in `lib/rag.ts` — `embedText` → `match_experiments` RPC → hydrate rows in similarity order. Returns [] with no key.
- [x] Merge/dedupe retrieved records (Map by id) → `formatRecord` context with `[EXP-###]` IDs.
- [x] **Generation**: `generateAnswer` — Claude (`claude-sonnet-5`) answers **only** from provided records, inline `[EXP-###]` citations (linked in UI) + Sources. Guardrail: no records → "No matching experiments found"; never-invent system prompt. Inert (null) without key.
- **Verify (deferred to Phase 10):** the 7 example questions (PDF §7) return correct, cited answers; an unanswerable question returns the no-match message. Until then: verify the keyless fallback still answers exact-filter questions.

## Phase 8 — AI summaries 🔑 (code only; activates in Phase 10)
Goal: grounded summaries, code-complete but inert without a key.
- [x] **Single-experiment summary**: `summarizeExperiment` (grounded, never-invent); `generateSummary(id)` server action caches into `ai_summaries` (scope `single`, `source_ids=[id]`); `SummaryCard` on detail page — Generate button only when key present, else "activates in Phase 10" note.
- [ ] **Group summary** (good-to-have): summarize the current filtered set. *(deferred — good-to-have; single-experiment done)*
- [x] Cache summaries; regenerate button; note model + timestamp. *(regenerate replaces the single-scope row; card shows model + date)*
- **Verify (deferred to Phase 10):** summary of EXP-001 mentions only its real fields; regenerating after an edit updates it; no invented values.

## Phase 9 — LLM-assisted entry 🔑 (code only; activates in Phase 10)
Goal: paste messy notes → pre-filled structured form (biggest adoption lever), inert without a key.
- [x] "Paste notes" box on New Experiment → `extractExperimentFields` (Claude → structured JSON, only stated fields, coerced/validated, methods restricted to the known set). `PasteNotes` disabled + Phase-10 note when no key.
- [x] Pre-fill the form for the **user to confirm/edit** before saving — `NewExperimentClient` sets extracted fields as form `initial` and remounts the form; nothing auto-saves (user still clicks Save).
- **Verify (deferred to Phase 10):** paste a realistic note → fields populate → user edits one → saved record matches edited values.

## Phase 10 — Add AI API keys → activate & verify AI (THE LAST PHASE)
Goal: buy/add keys once, flip everything on, verify, and add the eval story.
- [x] ~~Obtain Anthropic + OpenAI keys~~ → used a **free Gemini key** instead (single key covers chat + embeddings via the switchable provider layer).
- [x] Add to `.env.local` (gitignored) **and** Railway **dev** env; guards flip on automatically (`isLlmEnabled`/`isEmbeddingEnabled` read the active provider key).
- [x] Run the **embedding backfill** — 12/12 experiments embedded on `chememo-dev` (`scripts/backfill-embeddings.ts`, throttled 600 ms for free-tier).
- [x] Activate & verify **Phase 6/7** on dev (`scripts/verify-ai.ts`): semantic query "droplets/coacervate" → EXP-006/012/004 top by cosine; grounded answer cites [EXP-004/006/012] and excludes the fiber/non-droplet records in context (no hallucination). **Phase 8/9** chat path live (same `chatComplete`).
- [ ] **Retrieval eval set**: ~10 questions with expected experiment IDs + precision/recall script. *(pending)*
- [ ] Final deploy — dev is live on Gemini; **prod promotion still pending** (apply all migrations to `chememo` prod, set its env + Auth URLs). *(pending)*
- **Verify:** every 🔑 phase's deferred verify now passes on the live URL; eval script prints scores. AI is live.

---

## MVP cut line
**Keyless MVP = Phases 0–5.** That already satisfies the course PDF §10 for everything except the AI answer/summary, and is usable by the lab immediately at zero API cost. The AI layer (Phases 6–9 code + Phase 10 activation) upgrades it to the full RAG notebook once a key is available.

## Suggested order
0 → 1 → 2 → 3 → 4 → **5 (ship keyless notebook)** → build inert AI code 6 → 7 → 8 → 9 → **10 (add keys, activate, eval) LAST**. If a key appears earlier than expected, Phase 10 can simply be pulled forward — nothing else depends on its position.

---

## Appendix A — Data model (draft SQL, refine in Phase 2)
```sql
-- extensions
create extension if not exists vector;

-- profiles: one row per auth user
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  full_name text,
  initials text,
  created_at timestamptz default now()
);

-- projects: lab research programs (wet-dry, depsi, lcms, micro)
create table projects (
  id text primary key,            -- e.g. 'wet-dry'
  label text not null,
  color text
);

-- experiments: the core record
create table experiments (
  id text primary key,            -- e.g. 'EXP-001' (or use uuid + display code)
  name text not null,
  date date,
  researcher text,
  owner_id uuid references auth.users(id) default auth.uid(),
  project text references projects(id),
  reaction_type text,
  compounds text[] default '{}',
  metals text[] default '{}',
  ph numeric,
  concentration text,             -- free text (mixed units) for now
  temperature text,               -- free text (e.g. '60 C dry-down')
  cycles int,
  methods text[] default '{}',
  mz numeric[] default '{}',
  observations text,
  notes text,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  deleted_at timestamptz          -- soft delete
);

-- linked files: uploads (storage) OR external links
create table experiment_files (
  id uuid primary key default gen_random_uuid(),
  experiment_id text references experiments(id) on delete cascade,
  kind text check (kind in ('upload','link')),
  file_type text,                 -- 'excel','folder','image','pdf','spectrum'...
  label text,
  storage_path text,              -- for uploads (Supabase Storage)
  url text,                       -- for external links (OneDrive/Drive)
  created_at timestamptz default now()
);

-- embeddings for semantic search over free text (populated in Phase 10)
create table experiment_embeddings (
  experiment_id text primary key references experiments(id) on delete cascade,
  content text,                   -- the string that was embedded
  embedding vector(1536),         -- text-embedding-3-small
  updated_at timestamptz default now()
);

-- generated summaries (grounded; populated in Phase 10)
create table ai_summaries (
  id uuid primary key default gen_random_uuid(),
  experiment_id text references experiments(id) on delete cascade,
  scope text,                     -- 'single' | 'group'
  summary text,
  model text,
  source_ids text[],              -- experiment IDs used as evidence
  created_at timestamptz default now()
);
```
RLS sketch (Phase 2 detail): enable RLS on all tables; policy `read`: `auth.role() = 'authenticated' and deleted_at is null`; policy `write`: `owner_id = auth.uid()`.

## Appendix B — Target folder structure (from PDF §12, adapted)
```
chememo_webapp/
  app/ (login, dashboard, experiments, experiments/[id], new, ask)
  components/ (ExperimentForm, ExperimentTable, SearchChat, FileRow, ...)
  lib/ (supabaseClient.ts, rag.ts, embeddings.ts, auth.ts, anthropic.ts)
  database/ (schema.sql, seed.sql, policies.sql)
  README.md
```

## Appendix C — Watch-outs
- **AI keys are Phase 10 only.** Keep all Anthropic/OpenAI calls behind a guard that no-ops (or falls back to keyless filter search) when the key env var is absent — so the app never crashes pre-key.
- **Secrets**: `service_role` and (later) API keys are server-only — never expose in client code or commit them.
- **SQL from LLM output**: always parameterize; the LLM produces *filter params*, not raw SQL.
- **Embedding dim** must match the model (1536 for `text-embedding-3-small`) or pgvector errors.
- **Cross-session**: keep this note's Progress log current — it's the single source of "where we are".
