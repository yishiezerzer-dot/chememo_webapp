---
type: hub
project: ChemMemo
title: ChemMemo — Project Hub
status: shipped — live in production
priority: low
aliases:
  - ChemMemo
  - ChemMemo Hub
  - ChemMemo AI Lab Notebook
tags:
  - chememo
  - chememo/hub
  - moc
next_action: "**CI IS GREEN (2026-08-30, run 33308432622) — the first fully green run since 2026-08-03, roughly sixty runs ago.** `ci` and `rls` both pass: 21/21 e2e, 175 unit, 97 RLS. **CORRECTION to what this note said an hour ago: the sticky-state half did NOT cause the last failing test, and it was wrong to say so.** Reproduced locally and bisected: the a11y sort-header test fails identically at `f6a5446`, before any of 2026-08-30's work. It passes against deployed dev and fails on a cold local production build, which is what CI runs. **The cause was a hydration race in the test itself.** It pressed Enter immediately after `page.goto()`, and the header is a plain button element — an Enter arriving before React attaches its onClick is swallowed outright, nothing is queued, and no amount of waiting afterwards can rescue it. That is exactly the signature seen: the URL still on /experiments after the whole 15s, with no pending navigation to wait on. Two throwaway probes on the same build, one pressing Enter and one clicking, both passed once hydration had finished — so the product sorts correctly by keyboard and by mouse, and only the test was wrong. Fixed by retrying the activation until it takes (`79c9b91`), which still requires keyboard activation to produce sort=id and only stops the assertion assuming hydration completes inside one page-load tick. **That test had been quietly depending on the runner being fast enough, and only began failing when the suite and the runners changed around it; on a slow machine it never measured what its name says.** **So the whole fix for a month of red CI is: one hook (`useStickyState`, local-patch rendering with the reconciliation rule), the panels that use it, and one racy test.** PR #2's prefetch and sort-header rewrites were tried and reverted — they fixed nothing and cost a full page load per sort. **PR #2 can now be closed:** its diagnosis of vercel/next.js#93210 was right and its local-patch idea is what worked, but what is on dev differs in the rule that matters (the server wins the moment it moves, compared structurally rather than by reference) and drops the two changes that turned out to be dead weight. **What has NOT happened is Yishi's own top-to-bottom pass.** Green CI is not the same as correct: the sticky-state panels are new and largely unclicked by a human, and two self-inflicted bugs were made and fixed today — reference equality discarding every list patch, and 27 MB of QA binaries committed by a careless `git add -A` (untracked in `ac0818c`, still in history). **The promotion is now blocked only on that pass** — dev is 44 commits ahead, carrying one non-destructive migration that goes to prod first. Superseded context follows. — **STATUS 2026-08-30, end of session: CI is down from four failing specs to ONE, and the four that mattered are green — but it is not fully green and the last one is not understood.** What landed on dev (`93dcca3`): the local-patch rendering from PR #2, with its central rule inverted. The original latched a dirty flag and ignored server props until unmount; ours holds the patch only until the server says something genuinely new, compared STRUCTURALLY rather than by reference (a server re-render hands back a fresh array every time, and comparing by reference threw the patch away instantly — that was my bug, it broke the relationships panel on run 33297353128, and it is fixed with two tests for the list case). **comments-tasks, lifecycle, protocols and restore all pass** — first time since 2026-08-03 — along with auth, autosave, experiment, markdown-export, quantities, relationships, search and templates. 20 of 21 e2e assertions green; `rls` green. **The one remaining red is `a11y.spec.ts` — table sort header is keyboard-operable.** It times out with the URL never leaving `/experiments`. **My working theory about it was wrong and is recorded here so nobody re-runs it:** I assumed prefetch={false} was the cause and that reverting to sort buttons would restore it. It did not — the test fails identically with buttons and default prefetch, so the prefetch and sort-header rewrites were reverted (`93dcca3`) as they bought nothing and cost a full page load per sort. The remaining correlation is that the test passed for months, passed on 08-23, passed once at 3.1s on the cheap-half run 33296636320, and has failed every attempt since the sticky half landed — in BOTH the link and the button configuration. So something in the local-patch half breaks a router.push on the experiments list, and I do not have the mechanism. **Next step for it: reproduce locally rather than by CI cycle, and bisect the sticky half's files against that one spec.** Two self-inflicted mistakes this session, both caught and fixed, both worth knowing when reviewing: the reference-equality bug above, and 27 MB of QA screenshots and video committed by a careless `git add -A` (untracked and gitignored in `ac0818c`; the blobs remain in history and purging them needs a force push to a branch CI and Railway build from, so that is left as an explicit decision). **Yishi's top-to-bottom pass is still outstanding, and the promotion is still held for it — and the sticky-state panels are now the part most deserving of that pass, since they are new, they are mine as much as the agent's, and green CI is not the same as correct.** Superseded context follows. — **RESULT OF THE SPLIT EXPERIMENT (2026-08-30, run 33296636320): the cheap half is NOT the fix. The same four specs still fail — comments-tasks, lifecycle, protocols, restore — so PR #2's green CI is coming from `useStickyState`, not from `prefetch={false}`.** Two things did improve and are worth keeping: `relationships` went green (it had been failing intermittently since 2026-08-18) and the keyboard sort test now passes in 3.1s instead of ~5.3s, as a real link rather than a queued router.push. `quantities` failed once and passed on retry — a new flake worth watching. **So the decision Yishi has to make is now unavoidable and is exactly the one flagged: adopt optimistic client rendering, or accept a red CI.** Recommended middle path rather than PR #2 as written: keep the local patch, but let ANY later server value win and clear it, instead of ignoring the server until unmount. PR #2's hook sets a `dirty` flag that permanently suppresses server props; inverting that one line keeps the CI benefit (the assertion sees the patch immediately) while restoring server-as-truth the moment the refresh actually commits. That removes the objection — a lab notebook whose screen can diverge from the record indefinitely — without giving up green CI. Ready to implement on one word. Superseded context follows. — **NEXT ACTION (updated 2026-08-30): two bot deliverables were reviewed; the cheap half of PR #2 and the one real QA finding are now on dev, and CI is running on that split as a deliberate experiment.** **There are only ever two PRs on this repo** — #1, closed 2026-08-23, and #2, the open Cursor-agent one. The second bot deliverable is not a PR: it is a local `QA_REPORT.md` from an Antigravity run on 2026-08-26, now kept at `docs/ChemMemo_QA_Audit_2026-08-26.md`. **PR #2's diagnosis is correct and externally corroborated** — vercel/next.js#93210 is real, open, and a v16 regression: router.refresh() eagerly re-prefetches every in-viewport Link, which the reporter measured as a 50x ISR write amplification. That explains the twelve seconds of network silence we found independently and why a timeout bump would not help. Its CI green was verified by hand (run 33214298505, ci and rls). It also preserves the 2026-08-23 unstable_rethrow fix and correctly spots that the restore spec's cleanup was only reachable because Complete never updated the tree. **It was NOT merged whole, and the split is the point.** Taken (`427a519`): prefetch={false} on the always-visible chrome, sort headers converted from router.push buttons to real links (the thing the 2026-08-20 attempt got wrong), the a11y spec selector, and a new component test. **Left behind: `useStickyState` and the panel rewrites**, which render a mutation from client state and then deliberately ignore later server values until unmount. For a lab notebook whose whole worth is that the screen matches the record, and for a project bitten four times this month by exactly the UI-says-yes-database-says-no class, that is an architectural decision for Yishi to make after clicking the app — not a side effect of getting CI green. **If CI goes green on the cheap half alone, the expensive half is never needed; that is what the current dev run is testing.** restore.spec.ts was left alone too, since its rewrite depends on the half not taken. **The QA audit yielded one real item and two mis-filings** (`5a516cc`): security headers added (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy; HSTS deliberately omitted as it belongs at Railway's edge); the four lint warnings cleared, though NOT the way the audit said — it recommended deleting the unused `outsiderClient` fixtures, and checking first showed the cross-workspace negative is asserted from the member's side so they really are dead, but that analytical.test.ts's case was named for instruments AND runs while asserting only instruments, the same name-does-not-match-body trap that let the purity bug survive 162 green tests. Its DEF-HEALTH-01, filed P2 Major, is not a defect at all — operational state whose own fix is pressing an existing button. Its sort-header finding is vague but useful corroboration that the dropped-navigation problem is real outside CI. **Still true: Yishi has not done his own top-to-bottom pass, and the promotion is still held for it.** Superseded context follows. — **NEXT ACTION (updated 2026-08-26, Comprehensive QA Review & Evidence-Based Audit complete): 260 checks executed across the dev environment (https://chememowebapp-dev.up.railway.app), 253 passed (97.3%), 0 P0/P1 blockers. Release recommendation is CONDITIONAL GO for lab members.** All 4 local gates clean (typecheck, lint, build, vitest 164/164). All 11 audited routes pass Axe-Core WCAG 2.2 AA with zero critical/serious violations. All 5 responsive viewports (360x800, 390x844, 768x1024, 1366x768, 1920x1080) render clean without horizontal overflow. All primary user journeys (workspace management, experiment planning, blank creation, edit/save, samples/batches panel, and soft delete) verified end-to-end. **Three minor operational actions before production promotion:** (1) Click 'Re-index stale chunks' on /health to clear 33 stale embedding chunks and bring /api/health from degraded to OK. (2) Add standard HTTP security headers (HSTS, X-Content-Type-Options: nosniff, X-Frame-Options: DENY) to next.config.ts. (3) Proceed with Yishi's scheduled top-to-bottom manual lab walkthrough. Full QA report generated at QA_REPORT.md and vault audit note [[ChemMemo_QA_Sweep_2026-08-26]]. Superseded context follows."
updated: 2026-08-26
---

# 🧪 ChemMemo — Project Hub

> [!success] Status: LIVE IN PRODUCTION (Tier 1–3, all QA fixes) · AI provider OpenAI (gpt-5.6-luna) — **fully active, `OPENAI_API_KEY` added to prod 2026-08-17, confirmed via a real `ai_requests` row (`ask_general`, `status: ok`)**
> **[chememowebapp-production.up.railway.app](https://chememowebapp-production.up.railway.app)** · Phases 0–10 ✅ · Tier 1 (T1.1-T1.11) ✅ · Tier 2 (T2.1-T2.9) ✅ · **Tier 3 (T3.1-T3.8, all 5 T3.6 sub-items) ✅ promoted to master and live in production (2026-08-12)**, along with all 2026-08-11/12 QA-sweep fixes · full internal audit backlog ✅ · user-managed projects ✅ · **[[ChemMemo_QA_Sweep_2026-08-11]] — 2 P0 + 6 P1 bugs fixed & verified live, dev-DB test-data clutter mostly cleared (reversibly) — see §0.1**
> An AI-assisted lab notebook for prebiotic chemistry in the **MFP (Frenkel-Pinter) lab** — structured experiment records + linked analytical files + natural-language RAG over past experiments. *Goal: a tool the whole lab actually uses every day.*
>
> **2026-08-17:** AI Field Suggestions (see [[ChemMemo_Feature_AIFieldSuggestions_Spec]]) and the Planning Crew's D11 reliability fix (see [[ChemMemo_Feature_PlanningCrew_Spec]]) — shipped to dev and live-verified 2026-08-16, **fully promoted to production today** after two more rounds of live-testing bug fixes: D12 (a field-name mismatch in the crew's unresolved checklist) and D13 (suggestions matched checklist items by array position instead of field name, verified fixed via a deterministic SQL test). A loading-spinner convention, scoped to only the button actually clicked, was also rolled out app-wide and promoted alongside. Full details in [[ChemMemo_Implementation_Plan#Session history]].

> [!success] 2026-08-18 — QA sweep promoted: 13 commits live in production
> Everything below shipped to prod today. The headline fixes: "Resolve with AI" no longer clears the wrong checklist item; **semantic search works again for the first time since the move to OpenAI** (the similarity threshold had been tuned for Gemini and rejected every real hit — measured live, the eight correct chunks scored 0.46 down to 0.38 against a 0.5 cut-off); the three job queues can no longer strand a row forever; and `/api/health` is a usable signal instead of a permanent latch. Prod's own stranded chunk was reclaimed within 30 seconds of the deploy. Open next: EXP-001–012 are ownerless and so uneditable by anyone, and `MIN_SIM` wants re-measuring on the new model.

> [!warning] 2026-08-18 — QA sweep: the findings behind that promotion
> A click-through of dev found that **"Resolve with AI" still clears the wrong checklist item in production**. D13 (2026-08-17) made field name the join key between a suggestion and its crew unresolved item, assuming field name is unique — it is not, because the crew's four agents each append findings independently (a real draft had 28 items across only 16 distinct fields). Fixed on `dev` by giving every unresolved item a real uuid (**D14** in [[ChemMemo_Feature_AIFieldSuggestions_Spec]]) and verified end-to-end against the database; **not promoted — prod is still exposed.** Five further findings were then fixed in a second pass, also on `dev` only — most notably the job queues can no longer strand a row in `processing` forever, and `/api/health` **returned `status: ok` for the first time since 2026-08-09**, cleared through a new in-app Retry button rather than a manual SQL write. Only the e2e-test-data flooding remains open, and it needs a decision because every plausible fix deletes data. See the frontmatter `next_action` above and [[ChemMemo_Implementation_Plan#Session history]].

This is the **hub note (Map of Content)**. Start here each session.

---

## 🗺️ Map of content

### Vision & requirements
- [[ChemMemo_Characterization]] — what the app *is*: vision, MVP definition, good-to-haves, UX principles. The north star. *(reference)*

### Domain standard (governs Tier 1 and above)
- [[ChemMemo_MFP_Lab_Notebook_Standard]] — the **MFP lab's own record-keeping standard**: what a scientific record must contain, the object model, identifier and ratio rules, per-method analytical field lists, controlled vocabularies, and 11 Obsidian templates. The authority on *fields*; the evolution plan stays the authority on *order*. *(reference, v1.0)*
- [[ChemMemo_Standard_to_Plan_Crosswalk]] — **read before any Tier 1 item.** Maps every Tier 1–4 plan item to the standard sections that govern it, plus 12 gaps (**G8 and G11 closed 2026-07-29**, ten still open proposals) and the **5 conflicts, all decided 2026-07-29** — notably C3, which **added the missing Batch level to the data model**. §7 records each decision and what it binds. *(active, 0 open conflicts)*

### Plans & progress
- [[ChemMemo_Implementation_Plan]] — the phase-by-phase build plan (Phases 0–10) **and the full dated progress log**. This is the source of record for "what happened when." *(shipped)*
- [[ChemMemo_Product_Evolution_Plan]] — **priority-ordered, agent-executable roadmap** derived from the external comprehensive audit. Tiers the audit into Tier 0 (hardening) → Tier 4 (institutional). *(not started — read its scope callout before beginning)*

### Audits
- [[ChemMemo_QA_Sweep_2026-08-11]] — **near-closed backlog.** Full functional browser sweep of every reachable page/field on dev. 2 P0 + 6 P1 bugs fixed & verified live; 1 item (silent "Mark complete" feedback) turned out not to be a bug; only item 10 (dev-DB test-data cleanup, needs go-ahead) remains open. §0.1 is the fix-status board.
- [[ChemMemo_Audit_Roadmap]] — the **internal** audit + P0–P3 sprint roadmap of the built app. Every item is shipped; §0.1 is the at-a-glance board, §3 holds the small residual backlog. *(closed)*
- [[ChemMemo_Comprehensive_Product_Audit_2026-07-21]] — the **external** deep audit (product, UX, AI, security, data model, ELN benchmarking). The rationale behind the evolution plan. *(reference)*

### Specs (per-feature designs)
- [[ChemMemo_Feature_ProjectManagement_Spec]] — user-created/deletable projects (replaced the 4 hardcoded seed projects). *(shipped)*
- [[ChemMemo_Feature_ExperimentLifecycle_Spec]] — **T1.1**: experiment status, the completion lock, the §8.1 planning fields, and the §23 vocabulary seed table. The first Tier 1 item and the one that implements decisions C1/C2/C3/G11. *(draft — awaiting go-ahead)*
- [[ChemMemo_Feature_PlanningCrew_Spec]] — **T3.7**: the four-agent experiment planning crew, propose-only. TypeScript-native, no writes, no tools. *(implemented & live in production — shipped 2026-08-10, promoted 2026-08-12, reliability-fixed and re-promoted 2026-08-17)*
- [[ChemMemo_Feature_CrewAuthoredDrafts_Spec]] — **T3.8**: gives the crew an INSERT-only write path. Records land at `draft`, unresolved items gate leaving it, and the crew never writes acceptance criteria. *(implemented & live in production — shipped 2026-08-10, promoted 2026-08-12)*
- [[ChemMemo_Feature_IndexJobs_Spec]] — durable indexing job queue for semantic-search embeddings (T0.5). *(shipped — dev)*
- [[ChemMemo_Feature_AIFieldSuggestions_Spec]] — **AI field suggestions + "Resolve with AI"**: a general "what's missing" scan on any experiment, plus an AI-generated option alongside the crew's existing manual "Resolve" button, plus (added same day) automatic plan-time generation when a crew draft is created. Not on the roadmap — an ad hoc request. *(implemented, live-verified & shipped to production — 2026-08-17)*

> [!tip] Where to look
> - "What's the state of the app / what's left?" → **this hub** + [[ChemMemo_Audit_Roadmap]] §0.1 & §3
> - "What happened in a past session?" → [[ChemMemo_Implementation_Plan]] progress log
> - "What should we build next and how?" → [[ChemMemo_Product_Evolution_Plan]]
> - "Why build it that way?" → [[ChemMemo_Comprehensive_Product_Audit_2026-07-21]]
> - "What fields must a real lab record have?" → [[ChemMemo_MFP_Lab_Notebook_Standard]]
> - "Which of that applies to the item I'm about to build?" → [[ChemMemo_Standard_to_Plan_Crosswalk]]

---

## 🧭 Current state (2026-07-22)

- ✅ **Live in production**, feature-complete for daily lab use: auth, structured experiment CRUD, file upload/link + signed URLs, hybrid keyless + AI search, grounded cited Ask (streaming), AI summaries, paste-notes extraction, edit history, CSV export, mobile nav, user-managed projects.
- ✅ **Last shipped (2026-07-21):** user-managed projects on dev + prod (`c47384d`); 4 seed projects removed from prod.
- 🟡 **Known, non-blocking residuals** (see [[ChemMemo_Audit_Roadmap]] §3): CSV *import* never built (export only); "soft-delete orphans" (storage/`experiment_files` not cleaned on experiment delete); two a11y gaps (table sort headers, auth-toggle keyboard); one security-hardening item (rate-limit AI actions). *(Corrected 2026-08-19: "validate `addFileLink` URL schemes" was listed here as open but has been implemented for some time — [[ChemMemo_Audit_Roadmap]] §3 already recorded it done on 2026-08-04, and a live click-test re-confirmed it: submitting `javascript:alert(...)` as a file link stored nothing, because `validateLinkUrl()` in `lib/files/service.ts` allows only `https://` and rejects embedded credentials, server-side. [[ChemMemo_Product_Evolution_Plan]]'s T0.2 rationale still repeats the stale claim.)* Re-verify Ask AI/summary once Gemini `gemini-flash-latest` 503s subside (external/transient).
- ✅ **Tier 0 fully shipped on dev (2026-07-28) — 11/11 items done.** [[ChemMemo_Product_Evolution_Plan]] Tier 0 — T0.1–T0.4 on 2026-07-23, T0.5 on 2026-07-25, T0.6/T0.7/T0.8/T0.9 on 2026-07-26, T0.11 and T0.10's live verification both on 2026-07-28 (T0.2 closed a real stored-XSS hole, T0.4 closed a provenance risk, T0.5 is spec-first — [[ChemMemo_Feature_IndexJobs_Spec]]). **T0.11** tightened project-deletion RLS to owner-only and, discovering dev (unlike prod) still had 4 ownerless seed projects in active use, backfilled their ownership rather than leave them strandable — verified via a direct RLS test. **T0.10**'s live `/health` click-through (blocked since 2026-07-27 by a university network issue, now resolved) surfaced and fixed three real bugs in the health-metrics code: a stale null-model job could win the "latest embedding version" pick; `indexedCount` could exceed `totalExperiments` by counting soft-deleted experiments' jobs; and — the deeper one — `indexedCount` was sourced from `index_jobs` at all, which only tracks experiments touched by the T0.5 job queue (added 2026-07-25), so the 13 pre-existing seed experiments (real embeddings, never edited since) were invisible to it. Now sourced from `experiment_embeddings` directly; verified live against ground-truth SQL (14/14). Nothing promoted to master/prod as app code — all Tier 0 work is dev-only. Next: reassess with Yishi before Tier 1 (needs explicit go-ahead per the plan's gate); also worth deciding when to promote the accumulated Tier 0 work to master.

- ✅ **T1.1 (Experiment lifecycle & completion) shipped — live in production (2026-07-29).** Tier 0 (11/11 items) also promoted to master/prod the same session. See [[ChemMemo_Feature_ExperimentLifecycle_Spec]].

- ✅ **T1.2 (Templates & clone experiment) shipped — live in production (2026-08-02).** Template library with immutable-once-used versions, a 3-option new-experiment entry menu, and clone-with-section-selection. See [[ChemMemo_Feature_Templates_Spec]].

- ✅ **T1.3-T1.11 (the rest of Tier 1) promoted to master and live in production (2026-08-04).** All 7 pending migrations applied to prod first, `dev` merged into `master`, Railway deployed, `/api/health` confirmed healthy, and a full authenticated browser walkthrough of the real production site confirmed every feature below renders correctly with real data. **Tier 1 is now fully shipped and live** — the per-item bullets below are the historical dev-only record of when each was built; none of the "shipped to dev only" / "master push deferred" phrasing in them still applies.

- 🟡 **T1.3 (Autosave, draft recovery, conflict detection) shipped to `dev` only (2026-08-02).** New owner-only `experiment_drafts` table, two-tier autosave, optimistic-concurrency conflict detection. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_AutosaveDraftRecovery_Spec]].

- 🟡 **T1.4 (Structured quantities & units) shipped to `dev` only (2026-08-02).** New `quantity_kinds` registry + `experiments.quantities jsonb`; structured value+unit editor replaces the free-text temperature/concentration fields, legacy values kept as read-only display. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_StructuredQuantities_Spec]].

- 🟡 **T1.5 (Versioned protocols & experiment steps) shipped to `dev` only (2026-08-02).** New `protocols`/`protocol_versions`/`protocol_steps` (freeze-on-first-use) + `experiment_steps`/`step_observations`/`step_deviations` (append-only logs); new `/protocols` admin UI and a step-runner on the experiment detail page. `experiments.protocol_version_id` supersedes the old free-text field. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_VersionedProtocols_Spec]].

- 🟡 **T1.6 (Server-side experiment search & pagination) shipped to `dev` only (2026-08-03).** New trigger-maintained `search_vector tsvector` column (per-sample legacy codes included via `jsonb_to_tsvector`), generalized keyset pagination, owner-only `saved_views`. `/experiments` rewritten from full-client-side filtering to URL-state-driven. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_ServerSideSearch_Spec]].

- 🟡 **T1.7 (Experiment relationships & series) shipped to `dev` only (2026-08-03).** New `experiment_relationships` (8 typed bidirectional edges) + `experiment_series`/`experiment_series_members` (formal grouping + comparison page), both lab-shared. Clone now also records a `based_on` relationship. First item this pass to go fully green on the first CI run. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_ExperimentRelationships_Spec]].

- 🟡 **T1.8 (Improved revision diff & restore) shipped to `dev` only (2026-08-03).** Fixed the noise-revision trigger bug and the stale field-allowlist bug in the pre-existing history panel; new exclusion-based diff, merged revision/lock/file timeline with real editor identity, and a restore mechanism that inherits the completion lock for free. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_RevisionDiffRestore_Spec]].

- 🟡 **T1.9 (Comments, mentions, review requests) shipped to `dev` only (2026-08-03).** New `comments`/`comment_mentions`/`experiment_tasks`/`notifications`, task/review sharing one table, `task_status` seeded as an 8th `controlled_vocabularies` set. Fixed a `listTimeline`-shaped performance regression (Tasks/Comments now stream via their own Suspense boundary), an unscoped `profiles` fetch, and a WCAG AA color-contrast bug in the new notification badge. UI wired at the `experiment` target level only (step/file targets deferred, disclosed in the spec). CI's final push landed 12/14 e2e green after confirming the remainder was GitHub Actions/network variance rather than a code issue — shipped with Yishi's explicit sign-off rather than continuing to spend on retries. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_CommentsTasksReview_Spec]].

- 🟡 **T1.10 (Accessibility pass to WCAG 2.2 AA) shipped to `dev` only (2026-08-04).** Re-verified the prior audit's 5 a11y gaps against current code (2 already partially fixed by later features) before closing the rest: sort headers and the auth toggle are real `<button>`s with `aria-sort` now; save/upload states wired into `aria-busy`/`role=status`; light-mode `--ink-mute` darkened for WCAG AA contrast; Ask AI's loading/answer cards get `aria-live` regions; axe-core e2e scan extended to 2 more pages. No migration (UI/CSS only). All of this item's own tests landed clean on final CI runs; the one persistently red test (`protocols.spec.ts`) is the same pre-existing environmental flakiness from T1.9's rollout, unrelated to any file T1.10 touched. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. See [[ChemMemo_Feature_AccessibilityPass_Spec]].

- 🟡 **T1.11 (Single-record Obsidian Markdown export, read-only) shipped to `dev` only (2026-08-04) — the last Tier 1 item.** Renders one experiment against §20.2's template: quantities via the already-built `toStandardFieldName` helper, sections with no backing entity (batches/materials/analyses — Tier 2/3 work) omitted and named in a closing callout rather than silently dropped, plus relationships/tasks/deviations/change-log sections the standard's own template predates. No migration (pure read + render), reusing T1.6's CSV-export convention rather than a new route. This item's own e2e test passed cleanly; the rest of that suite run was the same pre-existing environmental variance from T1.9's rollout, now visibly wider, unrelated to any T1.11 file. Master push deliberately deferred to the Tier 1 boundary per Yishi's direction. **Tier 1 is now feature-complete on `dev` (11/11 shipped, none yet promoted to master).** See [[ChemMemo_Feature_MarkdownExport_Spec]] and the frontmatter `next_action` above.

- 🟡 **T2.1 (Workspace & role model) shipped to `dev` only, applied to `chememo-dev` (2026-08-05) — first Tier 2 item.** New `workspaces`/`workspace_members`/`project_members`/`invitations` tables + `workspace_role` enum; `workspace_id` added to every scientific table (7 top-level NOT NULL, 16 child trigger-populated); RLS rewritten across ~23 existing + 4 new tables via `is_workspace_member`/`_writer`/`_admin` security-definer functions; backfill into one `MFP Lab` workspace preserving today's access. Self-serve workspace creation/switching, members page, copy-paste invite links. CI caught two genuine security regressions the RLS rewrite introduced (not test bugs): `experiment_steps`/`step_observations`/`step_deviations` lost D9's owner-only write check, and `protocol_steps` lost its frozen-version check — both fixed in the migration source plus a dedicated follow-up migration for `chememo-dev` (already ran the buggy version once, idempotent migrations don't reapply). 11 of 12 pre-existing RLS test suites needed a shared workspace-setup helper since they predate workspaces. Master push deliberately deferred — awaiting Yishi's explicit go-ahead. See [[ChemMemo_Feature_WorkspaceRoleModel_Spec]].

- 🟡 **T2.2 (Materials, lots & stock solutions) shipped to `dev` only (2026-08-05) — second Tier 2 item.** New `materials`/`material_lots` (identity vs. per-purchase split), `material_identifiers` (kind/value, carries §6.4 aliases), `storage_locations`, `stock_solutions` + `stock_solubility_attempts` (the actual §7.4 multi-attempt log, distinct from the stock's own final outcome), `experiment_inputs`/`experiment_outputs` (polymorphic lot/stock reference with a cross-workspace guard, reusing T1.9's target_type/target_id pattern). New `mass`/`volume` unit-conversion families. `/materials` admin UI, an Inputs & Outputs panel on the experiment detail page, and the compounds/metals cutover to read-only display once real inputs exist. CI's `rls` job fully green on the first run; `ci` job's failures matched the same pre-existing environmental flakiness, none touching a T2.2 file. Master push deliberately deferred — awaiting Yishi's explicit go-ahead. See [[ChemMemo_Feature_MaterialsLotsStock_Spec]].

- 🟡 **T2.3 (Samples & lineage) shipped to `dev` only (2026-08-05) — third Tier 2 item.** New `batches` (every experiment auto-creates an implicit B1, per decision C3), `samples` (FK to a batch, never an experiment; §6.2 vial labels generated server-side from experiment/batch/material/replicate), `sample_aliases`, `sample_relationships` (T1.7's typed-edge pattern), `sample_locations` + `sample_events` (resolves G7's chain-of-custody gap by folding the transfer event in as one event_type rather than a new table), `sample_measurements`. `sample_type`/`reaction_mode`/`sample_status` (seeded since T1.1, finally consumed) plus a new `storage_status` vocabulary. `sample_matrix` (T1.2) deliberately untouched. CI caught a real bug — several server actions wired as plain closures instead of `.bind()` calls, rejected by Next.js at the server/client boundary — fixed before promotion was considered. CI's `rls` job fully green; `ci` job's failures matched the same pre-existing environmental flakiness, none touching a T2.3 file. Master push deliberately deferred — awaiting Yishi's explicit go-ahead. See [[ChemMemo_Feature_SamplesLineage_Spec]].

- 🟡 **T2.4 (Reaction & stoichiometry) shipped to `dev` only (2026-08-05) — fourth Tier 2 item.** Extended T2.2's `experiment_inputs`/`experiment_outputs` in place (moles/equivalents/is_limiting_reagent/calculation; theoretical_yield_mass/percent_yield/calculation) rather than a new duplicate table. Moles derived by stock concentration, solid mass/MW/purity, or liquid volume/density/MW/purity, whichever an input's data actually supports. Limiting reagent + equivalents computed only by an explicit Recalculate action, never automatically, per §18.6/§19.2's "never silently change a calculated value." Molar ratio (§6.6) and total reaction volume computed live, not stored. `sample_matrix`/`samples`/`batches` untouched. CI's `rls` job fully green; `ci` job's failures matched the same pre-existing environmental flakiness, none touching a T2.4 file. Master push deliberately deferred. See [[ChemMemo_Feature_Stoichiometry_Spec]].

- 🟡 **T2.5 (Analytical run model) shipped to `dev` only (2026-08-06) — fifth Tier 2 item, largest by field count.** New `instruments`/`instrument_methods`/`analysis_runs`/`analysis_files`/`analysis_results`/`peak_assignments`, hanging off `samples` (T2.3) rather than `experiment_outputs` (T2.2), per research-confirmed evidence (existing `analyzed_in` sample relationship, `measured` sample event, and §14's own "parent aliquot" language). Method-specific data starts as JSONB (`analysis_results.details`, `instrument_methods.parameters`, `analysis_runs.run_parameters`) per the plan's own instruction; `peak_assignments` is the one genuinely tabular exception since MS peaks are naturally multi-row. New `/instruments` admin registry; `samples-panel.tsx` extended with a self-contained `AnalysisRunsSection`. **CI caught a real bug**: a dead `export type { IonMode };` re-export in a `"use server"` file crashed nearly every server action at runtime (`ReferenceError`, ~20 e2e specs failed across unrelated files) — fixed by deleting the unused export; re-confirmed green apart from the usual pre-existing flakiness. Same push fixed a materials-tab bug Yishi reported mid-session: lots didn't show their storage location, and there was no way to delete a material or lot — added `deleteLot`/`deleteMaterial` (guarding against dangling `experiment_inputs` references and in-use materials) plus delete buttons and a location display in `materials-client.tsx`. Master push deliberately deferred. **All five Tier 2 items shipped this session (T2.1-T2.5) are dev-only, unpromoted.** See [[ChemMemo_Feature_AnalyticalRuns_Spec]].

- 🟡 **T2.6 (Prebiotic condition programs & controls) shipped to `dev` only (2026-08-06) — sixth Tier 2 item, the "prebiotic-chemistry differentiator."** New `condition_program_templates` + `batch_condition_programs` (a **frozen per-batch instance** once applied, mirrors T1.5's protocol-version freeze), `condition_program_cycles` (per-cycle actuals, a lightweight visual timeline), `environmental_conditions` (per-batch, core §9.4/§9.5 fields + an open-ended `custom_fields` map), `controls` (explicit entities at the experiment grain, reusing T1.7's existing `control_for` relationship type for linkage rather than a new table) + a `control_type` vocabulary. New `/condition-programs` admin page; a per-batch condition-program section in the samples panel; a Controls panel with a required-controls badge. Corrected a spec assumption mid-build: `reaction_mode` is per-sample (T2.3), not per-experiment, so the required-controls rule checks `hasConditionProgram` instead. `experiments.cycles` untouched; the old `controls` checklist goes read-only once real controls exist (mirrors T2.2's compounds/metals cutover). CI's `rls` job fully green; `ci` job's failures matched the same pre-existing environmental flakiness, none touching a T2.6 file. Master push deliberately deferred. **All six Tier 2 items shipped this session (T2.1-T2.6) are dev-only, unpromoted.** See [[ChemMemo_Feature_ConditionProgramsControls_Spec]].

- 🟡 **T2.7 (File versions, checksums, previews, metadata extraction) shipped to `dev` only (2026-08-07) — seventh Tier 2 item, data-model + status-tracking scope.** Real drag-drop/upload-progress/inline-preview UX would need new npm dependencies and a direct-to-Storage upload architecture change (today's upload is a Server Action with no byte-level progress available) — surfaced explicitly; Yishi chose to ship the data model now and defer that UI/architecture work. New `file_versions` (identity/version split reusing T1.5's protocols/protocol_versions pattern), replace-with-new-version (old versions/storage objects never deleted, per §16.4), `file_role`/`source_instrument`/`acquisition_timestamp`/`parsed_metadata`/`retention_state`, `analysis_run_id` link-to-run + unlinked-file inbox, and a `file_jobs` queue reusing T0.5's `index_jobs` pattern (CSV gets real text-extraction; XLSX/PDF/thumbnails resolve `not_applicable` until a real processor exists). Backfilled existing uploads with a synthetic version 1. CI's `rls` job fully green (including a new RLS test proving the enqueue trigger); `ci` job's failures matched the same pre-existing environmental flakiness, none touching a T2.7 file. Master push deliberately deferred. **All seven Tier 2 items shipped this session (T2.1-T2.7) are dev-only, unpromoted.** See [[ChemMemo_Feature_FileVersionsPreviews_Spec]].

- 🟡 **T2.8 stage 1 (Chemical structure identifiers + 2D rendering) shipped to `dev` only (2026-08-07) — eighth Tier 2 item, stage 1 of 4.** Widened `material_identifiers`' `identifier_type` to add `'inchi'` (T2.2 already had `'smiles'`/`'inchikey'`, no new table needed). New `MoleculeStructure` component renders a material's SMILES identifier as inline 2D SVG via `smiles-drawer`, dynamically imported (`ssr: false`, the first client-only-load pattern in this codebase). Two real bugs caught and fixed post-push: a broken `package-lock.json` from the new dependency (recurring issue class, memory updated with the broadened trigger), and a `react-hooks/set-state-in-effect` lint error only CI's `npm run lint` step catches (not local build/typecheck), which also surfaced a related retry-after-error bug in the same component. CI's `rls` job fully green; `ci` job's failures matched the same pre-existing environmental flakiness. Stages 2-4 (structure editor, reaction schemes, substructure search) remain separate future items per the audit's own sequencing. Master push deliberately deferred. **All eight Tier 2 items shipped this session (T2.1-T2.8) are dev-only, unpromoted.** See [[ChemMemo_Feature_ChemicalStructureSupport_Spec]].

- ✅ **T2.9 (Matrix / series comparison view) shipped to `dev` only (2026-08-08) — ninth and last Tier 2 item. Tier 2 (T2.1-T2.9) is now fully shipped to `dev`, none promoted to master.** No field anywhere in the schema distinguishes compound identity — the matrix view pivots over `sample_matrix.component_1`/`component_2` (free text, matching the Standard's §21.2 worked example) plus `reaction_mode`/`sample_type` (controlled vocabularies), disclosing the free-text near-duplicate caveat in the UI rather than inventing a compound-classification schema addition. One shared `ComparisonTable` component serves both an enriched series comparison page (timeline-ordered by `experiments.date`) and a new relationship-based compare view grouped by relationship type. New `/experiments/matrix` page. No migration, no new RLS surface. CI's `rls` job fully green; `ci` job's failures matched the same pre-existing environmental flakiness, and `relationships.spec.ts` — the e2e test most exercising this item's changed files — passed cleanly. Master push deliberately deferred. See [[ChemMemo_Feature_MatrixSeriesComparison_Spec]].

- ✅ **Tier 2 (T2.1-T2.9) promoted to master and live in production (2026-08-09).** All nine items above are now deployed, not just dev-only — the "master push deliberately deferred"/"dev only" phrasing in the individual bullets above is historical (describes when each item was first built), not current status. Promotion surfaced and fixed two real issues: production's migration-tracking bookkeeping had silently desynced for T1.2-T1.9 (the schema itself was fine, verified against real data), and T2.1's `invitations` migration needed an explicit `search_path` fix for `pgcrypto` that chememo-dev didn't need. Confirmed live via `/api/health` and Railway's own deploy logs (T2.7's `file-jobs` poller starting cleanly — proof the new code, not a stale build, is running).

> Full dated history lives in [[ChemMemo_Implementation_Plan#Session history]] — not duplicated here.

---

## 🔧 Locked decisions (2026-07-01)

| Decision | Choice | Why |
|---|---|---|
| Framework | **Next.js + React** (App Router, TypeScript) | Port the polished mockup; real deployable multi-user tool. |
| Backend / DB / Auth | **Supabase** (Postgres + Auth + Storage + pgvector) | One platform for DB, auth, files, and vectors. |
| LLM (answers/summaries) | **Switchable** via `AI_PROVIDER` — currently **Gemini** (`gemini-flash-latest`) | Free key available; OpenAI/Anthropic paths kept. |
| Embeddings | **Gemini `gemini-embedding-001` @ 1536 dims** → pgvector | Matches `vector(1536)`; OpenAI `text-embedding-3-small` also wired. |
| Retrieval | **Hybrid**: deterministic structured filters **+** semantic search | Reliable for `pH>8`/compound queries; semantic for free-text. |
| File storage | **Upload small, link big** | Images/spectra → Supabase Storage; big LC-MS folders → OneDrive/Drive links. |
| Deploy | **Railway** (app) + Supabase (backend); `master`→prod, `dev`→dev | Simple Next.js deploys, public demo URLs. |
| Multi-user | **Lab-shared** (read-all, edit-own) via RLS | Matches "the whole lab uses it." *(The evolution plan revisits this with workspaces.)* |

---

## 🔑 Environments & secrets checklist

> [!danger] Never commit real keys to git or this vault. Secrets live only in `.env.local` (gitignored) and Railway env.

- [x] **Prod** Supabase `chememo` (ref `iazuubcyxneavrahjgww`, eu-central-1) — live DB, all migrations applied, `projects` seed kept, demo experiments dropped. Keys in Railway prod env only.
- [x] **Dev** Supabase `chememo-dev` (ref `khkpqnpmhravdpbogqai`, eu-central-1). Keys in Railway dev env. *(Re-fetch: `supabase projects api-keys --project-ref khkpqnpmhravdpbogqai`.)*
- [x] **Railway** project `chememo_webapp` — prod: https://chememowebapp-production.up.railway.app · dev: https://chememowebapp-dev.up.railway.app · GitHub auto-deploy `master`→prod, `dev`→dev.
- [x] **GitHub** private repo — https://github.com/yishiezerzer-dot/chememo_webapp
- [x] **AI provider** — dev env: OpenAI (`gpt-5.6-luna`), key already set. **Prod env: `AI_PROVIDER=openai` + `OPENAI_CHAT_MODEL=gpt-5.6-luna` set 2026-08-12, but `OPENAI_API_KEY` still needs to be added by Yishi** (entering credentials isn't something the assistant does itself) — until then prod's AI features fall back to keyless search. Switch providers via `AI_PROVIDER` + matching key.

> [!warning] Railway deploy gotcha (learned 2026-07-21)
> `railway service source connect` is **not** reliably environment-scoped despite `--environment`; it can reset the branch mapping for *both* environments at once. Verify dev↔`dev` and prod↔`master` mappings in the Railway **dashboard**, not via that CLI command. To deploy a branch to one environment without touching the other, use `railway up`.

---

## 🗂️ Vault structure

This project folder (`ChemMemo/`) inside the `Claude_code_projects` Obsidian vault is organized as:

```
ChemMemo/
├── ChemMemo.md                      ← this hub (entry point / MOC)
├── Vision/                          ← what the app should be
│   └── ChemMemo_Characterization.md
├── Plans/                           ← build plans + progress
│   ├── ChemMemo_Implementation_Plan.md
│   └── ChemMemo_Product_Evolution_Plan.md
├── Audits/                          ← reviews & roadmaps
│   ├── ChemMemo_Audit_Roadmap.md
│   └── ChemMemo_Comprehensive_Product_Audit_2026-07-21.md
├── Standards/                       ← domain rules the app must satisfy
│   ├── ChemMemo_MFP_Lab_Notebook_Standard.md
│   └── ChemMemo_Standard_to_Plan_Crosswalk.md
└── Specs/                           ← per-feature design specs
    └── ChemMemo_Feature_ProjectManagement_Spec.md
```

> [!note] Conventions for new notes
> - **Filenames keep the `ChemMemo_` prefix** — wikilinks resolve by filename vault-wide, so files can move between folders without breaking links, and the prefix disambiguates in global search / graph view.
> - **Frontmatter** every note with: `type` (`hub`/`vision`/`plan`/`audit`/`spec`/`standard`), `project: ChemMemo`, `title`, `status`, `tags` (`chememo` + `chememo/<type>`), and `created`/`updated` dates.
> - **`Standards/` holds domain requirements, not build plans.** A note goes here when it describes *what a scientific record must contain* rather than *what we implement next*. It is the lab's document; the app conforms to it, not the other way round.
> - **New per-feature designs** → `Specs/` as `ChemMemo_Feature_<Name>_Spec.md`, linked from the Map of Content above.
> - After finishing a work item, tick it in the relevant plan/audit **and** append a dated line to [[ChemMemo_Implementation_Plan#Session history]].

---

## 📎 Source material
- Original concept PDF: `AI course final project/ChemMemo_AI_Lab_Notebook_Project_Updated.pdf`
- Original clickable mockup (vanilla HTML/CSS/JS, 6 screens): `AI course final project/chemmemo-design/`
- Repo docs mirror: `chememo_webapp/docs/` (point-in-time snapshots of some of these notes)
